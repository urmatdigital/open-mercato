---
title: System status variables
description: Summary of the environment toggles surfaced in the admin System status view.
---

> The System status page reflects read-only snapshots of process environment variables. Update values by exporting them in your shell or editing `.env`, then restart the app so Next.js and background workers load the new configuration.

## Managing variables {#managing-variables}

- **Temporary override:** `export VAR_NAME=value && yarn dev`
- **Persistent change:** edit `.env` (or `.env.local`) and restart the application.
- **Secrets:** prefer `.env.local` or deployment-specific secret managers so changes stay out of version control.

The sections below cover the System status panel and local operational telemetry controls, along with the default values bundled with the platform. Follow the related links for deeper feature documentation where available.

## Module telemetry {#module-telemetry}

The **Module telemetry** admin page lives at `/backend/config/module-telemetry`. It reports module-level resource usage collected in the current process and any fresh local process telemetry files. Data is stored in 5-minute buckets; each bucket includes its own interval so older persisted buckets remain readable if the interval changes later.

Telemetry stages:

- `startup`: a bucket that overlaps the first 5 minutes after telemetry starts.
- `running`: buckets after startup.

Metric semantics:

- **Calls** and **CPU** are summed across selected buckets.
- **Heap / hour** and **RSS / hour** are positive growth pressure normalized by the selected time window.
- Heap and RSS growth are not live memory gauges. They track positive deltas observed after module operations, so they can grow even when current process memory later drops.

### OM_MODULE_RESOURCE_USAGE {#om_module_resource_usage}
**Default:** `true`

Enables local module resource attribution for API routes, event subscribers, queue workers, and custom tracked operations. Disable only when debugging the tracker itself or when no local telemetry should be collected.

### OM_MODULE_RESOURCE_USAGE_SNAPSHOT {#om_module_resource_usage_snapshot}
**Default:** `true` outside tests

Writes best-effort local process telemetry files so the admin page can include fresh data from sibling processes after restarts or worker activity. The shared runtime default is `.mercato/module-resource-usage`; managed local development uses the app-local path described below.

### OM_MODULE_RESOURCE_USAGE_DIR {#om_module_resource_usage_dir}
**Shared runtime default:** `./.mercato/module-resource-usage`

**Managed local-dev default (`yarn dev`):** `<app>/.mercato/next/module-resource-usage`

Overrides the directory used for local process telemetry files. Managed local development places snapshots below the app's Next.js output directory so telemetry writes do not trigger source rebuilds. A non-empty explicit value is preserved unchanged. Production and processes started outside the managed dev wrapper continue to use the shared runtime default.

### OM_MODULE_RESOURCE_HEAVY_P95_MS {#om_module_resource_heavy_p95_ms}
**Default:** `5000`

Threshold for flagging modules with slow p95 operation duration.

### OM_MODULE_RESOURCE_HEAVY_CPU_MS {#om_module_resource_heavy_cpu_ms}
**Default:** `25000`

Threshold for flagging modules with high accumulated CPU time.

### OM_MODULE_RESOURCE_HEAVY_HEAP_BYTES {#om_module_resource_heavy_heap_bytes}
**Default:** `262144000`

Threshold for flagging modules with high positive heap growth.

### OM_MODULE_RESOURCE_HEAVY_RSS_BYTES {#om_module_resource_heavy_rss_bytes}
**Default:** `262144000`

Threshold for flagging modules with high positive RSS growth.

### OM_MODULE_RESOURCE_HEAVY_ERRORS {#om_module_resource_heavy_errors}
**Default:** `10`

Threshold for flagging modules with repeated tracked operation errors.

## Profiling filters {#profiling}

### OM_PROFILE {#om_profile}
**Default:** _(unset)_  
Enables the backend tree profiler for requests matching the provided comma-separated filters. Useful for inspecting slow paths in server handlers. Related docs: [Profiling](https://docs.openmercato.com/docs/framework/performance/profiling).

### NEXT_PUBLIC_OM_PROFILE {#next_public_om_profile}
**Default:** _(unset)_  
Activates the tree profiler in the browser bundle so client-rendered flows can emit the same snapshots. Set alongside `OM_PROFILE` when debugging end-to-end latency.

### OM_CRUD_PROFILE {#om_crud_profile}
**Default:** _(unset)_  
Targets CRUD factory operations with fine-grained profiling filters. Enables `[crud:profile]` payloads for measuring database access patterns.

### OM_QE_PROFILE {#om_qe_profile}
**Default:** _(unset)_  
Turns on hybrid query engine profiling. Use it to verify whether requests hit the JSONB index or fall back to relational queries.

## Logging {#logging}

### QUERY_ENGINE_DEBUG_SQL {#query_engine_debug_sql}
**Default:** `false`  
Logs every SQL statement dispatched by the hybrid query engine. Enable temporarily when troubleshooting filters or sorting behaviour. Related docs: [Query index](https://docs.openmercato.com/docs/framework/database/query-index).

### LOG_VERBOSITY {#log_verbosity}
**Default:** _(unset)_  
Overrides structured log verbosity (`debug`, `trace`, etc.) across the backend. Leave blank to inherit module defaults or per-service settings.

### LOG_LEVEL {#log_level}
**Default:** _(unset)_  
Fallback log level when verbosity is not specified. Common values: `info`, `warn`, `error`.

## Cache controls {#caching}

### ENABLE_CRUD_API_CACHE {#enable_crud_api_cache}
**Default:** `false`  
Toggles the CRUD API response cache layer. When enabled, GET requests served by the CRUD factory reuse cached payloads within the TTL window.

### CACHE_STRATEGY {#cache_strategy}
**Default:** `memory`  
Selects the cache backend. Supported values include `memory` (in-process) and `sqlite`. Related docs: [Cache tuning](https://docs.openmercato.com/docs/framework/performance/cache).

### CACHE_TTL {#cache_ttl}
**Default:** _(unset)_  
Optional override for cache time-to-live in milliseconds. Leave blank to use per-handler defaults.

### CACHE_SQLITE_PATH {#cache_sqlite_path}
**Default:** `./.mercato/cache/cache.db`
Filesystem path for the SQLite cache file used when `CACHE_STRATEGY=sqlite`. Ensure the process has write permissions at this location.

## Authentication {#authentication}

### OM_PASSWORD_MIN_LENGTH {#om_password_min_length}
**Default:** `6`  
Minimum length enforced for new passwords (onboarding, user CRUD, password reset, CLI). To keep the frontend requirement hint aligned, set `NEXT_PUBLIC_OM_PASSWORD_MIN_LENGTH` to the same value.

### OM_PASSWORD_REQUIRE_DIGIT {#om_password_require_digit}
**Default:** `true`  
Require at least one digit in new passwords. Mirror in `NEXT_PUBLIC_OM_PASSWORD_REQUIRE_DIGIT` to surface the requirement in frontend helpers.

### OM_PASSWORD_REQUIRE_UPPERCASE {#om_password_require_uppercase}
**Default:** `true`  
Require at least one uppercase letter in new passwords. Mirror in `NEXT_PUBLIC_OM_PASSWORD_REQUIRE_UPPERCASE` for frontend display.

### OM_PASSWORD_REQUIRE_SPECIAL {#om_password_require_special}
**Default:** `true`  
Require at least one special character in new passwords. Mirror in `NEXT_PUBLIC_OM_PASSWORD_REQUIRE_SPECIAL` for frontend display.

## Query index maintenance {#query-index}

### SCHEDULE_AUTO_REINDEX {#schedule_auto_reindex}
**Default:** `true`  
Schedules background reindex jobs automatically when coverage gaps are detected. Disable if you prefer to handle reindexing manually through the CLI or queue workers.

### OPTIMIZE_INDEX_COVERAGE_STATS {#optimize_index_coverage_stats}
**Default:** `false`  
Caches coverage statistics instead of recalculating them on every read. Suitable for large datasets where immediate precision is less critical.

### OM_LIST_COUNT_CAP {#om_list_count_cap}
**Default:** `10000`
Upper bound on the row count a list request will establish. Below the cap, `total` is exact and nothing changes. At the cap the response reports `total: <cap>` plus `totalIsCapped: true`, and the UI renders it as `10 000+` — so above the cap `total` is a floor, not a value, and `totalPages` is a bounded page count rather than the end of the data. An exact count over arbitrary filters costs `O(matching rows)`, which is what makes large filtered lists slow; bounding it keeps list latency flat as tables grow. Set `0` to disable capping entirely and restore exact counts — a permanently supported escape hatch that takes effect without a redeploy. Related docs: [Query index](https://docs.openmercato.com/docs/framework/database/query-index).

### FORCE_QUERY_INDEX_ON_PARTIAL_INDEXES {#force_query_index_on_partial_indexes}
**Default:** `true`  
Allows the hybrid engine to serve results even when coverage is partial. Responses include an `x-om-partial-index` header so admins can schedule a rebuild. Related docs: [Query index](https://docs.openmercato.com/docs/framework/database/query-index).

### QUERY_INDEX_WARMUP_ENABLED {#query_index_warmup_enabled}
**Default:** `true`  
Kill switch for the `query_index.coverage.warmup` sweep fired after login. Set to `false` to disable the proactive warmup entirely and rely solely on the query engine's lazy, on-demand coverage refresh. Related docs: [Query index](https://docs.openmercato.com/docs/framework/database/query-index).

### QUERY_INDEX_WARMUP_THROTTLE_MS {#query_index_warmup_throttle_ms}
**Default:** `300000` (5 minutes)  
Minimum time between warmup sweeps for the same entity type and tenant, checked both against an in-process cache and the persisted coverage snapshot so a process restart doesn't repeat the full sweep.

### QUERY_INDEX_WARMUP_CONCURRENCY {#query_index_warmup_concurrency}
**Default:** `10`  
Number of `coverage.refresh` events dispatched per chunk during the warmup sweep. Lower this alongside `QUERY_INDEX_WARMUP_STAGGER_MS` to reduce peak concurrent database connections on memory-constrained deployments.

### QUERY_INDEX_WARMUP_STAGGER_MS {#query_index_warmup_stagger_ms}
**Default:** `0` (unchanged burst behaviour)  
Delay in milliseconds added between successive warmup chunks. A non-zero value (e.g. `2000`) spreads the post-login coverage burst over time instead of firing every stale entity type at once, lowering peak RSS/connection usage without changing the total number of refreshes.

## Entity compatibility {#entities}

### ENTITIES_BACKCOMPAT_EAV_FOR_CUSTOM {#entities_backcompat_eav_for_custom}
**Default:** `false`  
Mirrors custom-entity writes into the legacy EAV tables. Enable only when migrating integrations that still depend on the older schema.
