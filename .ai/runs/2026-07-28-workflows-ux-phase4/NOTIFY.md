# NOTIFY — workflows-ux-phase4

Append-only UTC log. Checkpoint events, blockers, and scope decisions only.

- 2026-07-28T09:00Z — Run started. Scope: Phase 4a of the workflows UX redesign, on branch feat/workflows-ux-phase4 off feat/agent-orchestrator-mvp @1f4ec94a6.
- 2026-07-28T09:05Z — Research briefing found NINE pre-existing defects. Two (A2 cross-tenant claim write, A3 complete-anyone) were fixed and merged separately as #4573 before this run began.
- 2026-07-28T09:30Z — SCOPE DECISION: six items deliberately excluded from this run because each needs a maintainer decision rather than an implementer's judgment — the §6.4 permission flip (spec requires a dedicated security review; BC has no rule for changing a shipped route's auth semantics; "entity access" is not a platform primitive), portal task API (portal RBAC is Ask-First), any new UserTaskStatus (state machines are Ask-First), the auto-approve/USER_TASK boundary (Ask-First), a shared LocalizedString type (new STABLE surface), and the C3 Task entity. Recorded in PLAN.md Non-goals.
- 2026-07-28T09:30Z — SEQUENCING: A1 (userTaskConfigSchema strips assignedToRoles, so Studio role assignment is silently discarded on save) is a hard prerequisite and lands as step 0.1. Every claim/role-queue story depends on it.
- 2026-07-28T11:30Z — Checkpoint 1: PHASE 0 COMPLETE (6/22 steps). All six pre-existing defects fixed, each with tests verified to fail against the unfixed code. 1983 tests green.
- 2026-07-28T11:30Z — CORRECTION to the briefing: auth.roles is server-derived, NOT client-spoofable. The role-names exposure is renames orphaning assignments, not spoofing. Names->ids migration deferred as a data-modelling task with explicit reasoning.
- 2026-07-28T11:35Z — Visual-fidelity analysis delivered (.ai/analysis/2026-07-28-canvas-visual-fidelity.md): 14 gaps (10 S, 2 M, 2 L). Found a real bug: strokeDasharray '5,5' on the DEFAULT edge state means every connection is dashed, so dashing signals nothing. Two maintainer observations corrected (mockup has no grouping boxes; built editor already ships Note/Group annotations; nodes are wider than the design, not smaller).
- 2026-07-28T14:00Z — Checkpoint 2 (11/22 steps). 2090 tests green.
- 2026-07-28T14:00Z — DECISION: new task configs mint `deadline` (the spec's canonical superset); existing configs keep `slaDuration` untouched, no migration. Rollback exposure recorded.
- 2026-07-28T14:00Z — DECISION: resolved decisions get NO column. Rendering the buttons is derived state (re-resolve from the instance's pinned definition); recording WHICH button was pressed is a separate audit fact captured at completion. Two requirements, one name — separated deliberately.
- 2026-07-28T17:00Z — MAINTAINER DECISIONS: (1) ship the §6.4 permission flip in FULL, one move — agent_orchestrator is not production yet so disposition-task invisibility is not a blocker (but 5.6 still gives them an administrative-queue class; "acceptable risk" is not "leave it broken"). (2) portal principals get an `assignee_kind` column, not a string prefix. Phases 5 and 6 added to the Tasks table; design at .ai/analysis/2026-07-28-task-visibility-design.md.
- 2026-07-28T17:00Z — Carrying two design deviations: D-1 keep claim/complete requireFeatures (dropping them strands two FROZEN ACL ids; portal parity is served by the new portal routes instead) and D-2 denormalized entity_types column so the gate is SQL, not a post-filter that makes pagination counts lie.
- 2026-07-28T17:05Z — FINDING (4th false brief premise caught by an executor, and this one was mine): my 2.5 brief said to write a CustomerTodoLink row from the workflows module, which would have meant workflows importing a customers entity and writing a customers table — banned by both root and core AGENTS.md. The executor inverted the dependency instead: workflows.task.assigned carries entityBindings, customers subscribes and writes its own link. Verified: no customers import exists in workflows.
- 2026-07-28T17:05Z — Steps 2.4-2.5 landed (15/22 of the original plan; 27 steps total now). Chosen decision recorded in form_data.__decision (reserved key, mirrors context.__error) plus the USER_TASK_COMPLETED event; the button LIST stays derived per checkpoint decision #2.
- 2026-07-28T20:00Z — Checkpoint 3: PHASE 4a FEATURE WORK COMPLETE (19/27). 2302 tests green.
- 2026-07-28T20:00Z — NEAR MISS: step 3.4's brief (mine) asked for required-field validation on the {fields:[]} form shape. That shape was ALSO never rendered — TaskFormFields only walked formSchema.properties. Validation alone would have made every Studio-authored task uncompletable. Executor caught it and fixed both halves through one pure normalizer.
- 2026-07-28T20:00Z — CONSTRAINT FOUND: notification quick-actions cannot carry WHICH decision was pressed — executeAction drops actionId. One-click is therefore limited to single-decision tasks; multi-decision takes the deep link. Fixing it properly is a notifications-module contract change, deliberately not made unasked.
