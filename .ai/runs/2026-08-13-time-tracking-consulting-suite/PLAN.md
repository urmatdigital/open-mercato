# Time Tracking — Consulting Suite · Build Plan

**Spec:** [`.ai/specs/2026-08-12-time-tracking-consulting-suite.md`](../../specs/2026-08-12-time-tracking-consulting-suite.md)
**Design authority:** [`.ai/mockups/time-tracking/`](../../mockups/time-tracking/) (17 screens)
**Module:** `packages/core/src/modules/staff`
**Started:** 2026-08-13

## How this file works

One row per task. `Status` is the single source of truth for progress and is updated **as work lands**, not in advance. A task is `done` only when its own verification command has been run and passed.

| Symbol | Meaning |
|---|---|
| `todo` | not started |
| `wip` | subagent dispatched, not yet verified |
| `done` | code landed **and** its verification passed |
| `gate-blocked` | code landed and reviewed, but its verification cannot run yet for a reason outside the task |
| `blocked` | needs a decision or an upstream task |

**Owner** names the subagent that executed it. **Verify** is the exact command that must pass.

Tasks within a phase that touch disjoint files run in parallel; tasks sharing a file are sequenced. The `Files` column is the conflict key — two `wip` tasks must never list the same file.

**Known shared surfaces the `Files` column does not capture:** `i18n/*.json` and `UPGRADE_NOTES.md` are appended to by many tasks. Agents touching them must read-modify-write and verify a concurrent agent's keys survived, rather than rewriting the file wholesale. T2.1 and T2.3 both wrote i18n concurrently and both sets of keys survived — by care, not by design.

---

## Phase 1 — Foundations (no user-visible change)

| Id | Task | Files (conflict key) | Depends | Status | Verify |
|---|---|---|---|---|---|
| T1.1 | 10 new entities + 11 additive columns in `data/entities.ts`; scoped migration + `.snapshot-open-mercato.json` | `data/entities.ts`, `migrations/` | — | **done** (R1 fixed) | `turbo run typecheck --filter=@open-mercato/core` |
| T1.2 | Pure helpers: `duration`, `interval`, `rounding`, `cost`, `rollup`, `overlap`, `projectCode` + unit tests | `lib/time-tracking/*` (excl. settings, access) | — | **done** — 104 tests pass | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff/lib/time-tracking` |
| T1.3 | Zod schemas for tasks, statuses, comments, tags, reports, settings in `data/validators.ts` | `data/validators.ts` | — | **done** | `yarn typecheck` (baseline) |
| T1.4 | 7 ACL features, `setup.ts` role grants, new event ids in `events.ts` | `acl.ts`, `setup.ts`, `events.ts` | — | **done** | `yarn generate && yarn typecheck` |
| T1.5 | `settings.ts` (ModuleConfigService-backed) + `GET/PUT /api/staff/timesheets/settings` | `lib/time-tracking/settings.ts`, `api/timesheets/settings/` | T1.2, T1.3 | **done** — 13 tests | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff/api/timesheets/settings` |
| T1.6 | `access.ts` resolver + DI registration in `di.ts` | `lib/time-tracking/access.ts`, `di.ts`, `AGENTS.md` | T1.2 | **done** — 119 helper tests, 278 staff tests | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff/lib/time-tracking` |
| T1.8 | **D-12 assignment window + grace.** New setting `access.assignmentGraceDays` (default 14); `resolveProjectAccess` enforces `assigned_start_date <= today <= assigned_end_date + grace` | `lib/time-tracking/access.ts`, `lib/time-tracking/settings.ts`, `data/validators.ts` | T1.6 | **done** — 332 staff tests (305 + 27) | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff` |
| T1.7 | Phase gate: full validation sweep | — | T1.1–T1.6 | **done** — test 29/29, lint 0 errors, typecheck at baseline | `yarn generate && yarn typecheck && yarn lint && yarn test` |

## Phase 2 — Projects, team, navigation (screens 3, 4, 5, 17)

| Id | Task | Files | Depends | Status | Verify |
|---|---|---|---|---|---|
| T2.1 | `backend/staff/time-tracking/` page tree + "Time tracking" nav group + 308 redirects from `timesheets/*` + `UPGRADE_NOTES.md` | `backend/staff/time-tracking/**`, `UPGRADE_NOTES.md`, `auth/lib/backendChrome.tsx`, `i18n/*` | T1.7 | **done** — build:app clean, 312 core tests | `yarn build:packages && yarn build:app` |
| T2.2 | Project form: customer picker (`customer_entities`) + snapshot, auto-derived editable code, rate, currency, billable default, budget card | `backend/staff/time-tracking/projects/{create,[id]/edit}/**`, `projectFormConfig.ts`, `lib/time-tracking-ui/*` | T2.1 | **done** — UI complete; blocked on T2.9 for persistence | TC-TT-004 (after T2.9) |
| T2.3 | Currency lock + `POST .../time-projects/[id]/change-currency` | `api/timesheets/time-projects/[id]/change-currency/`, `commands/timesheets-projects.ts`, `api/timesheets/time-projects/route.ts` | T1.7 | **done** — 305 staff tests | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff` |
| T2.4 | Projects list: Rate / Hours / Cost / Budget-burn columns, bulk assign + report-from-selected | `backend/staff/time-tracking/projects/page.tsx`, `lib/timesheets-projects*`, `data/enrichers.ts` | T2.1 | **done** — 384 staff tests | TC-TT-003 |
| T2.5 | Project team drawer: multi-select, TL locked row, remove-with-hours confirm | `lib/time-tracking-ui/ProjectTeamDrawer.tsx`, `projectTeamAssignment.ts`, `projects/[id]/page.tsx` | T2.1 | **done** — 485 staff tests (+23) | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff` |
| T2.6 | No-access guard state (screen 17) + `POST .../access-requests` + notification | `lib/time-tracking-ui/NoProjectAccess.tsx`, `api/timesheets/access-requests/`, `notifications.ts`, `subscribers/` | T1.6 | **done** — component + route + notification; **wired by the R3 fix** | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff/api/timesheets/access-requests src/modules/staff/subscribers` |
| T2.7 | Budget threshold event + notification | `subscribers/`, `notifications.ts`, `lib/timesheets-projects/budgetThreshold*.ts` | T1.4 | **done** — 477 staff tests (+47) | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff` |
| T2.9 | **Project write path for the new columns** — `hourlyRate`, `currencyCode` (create-only), `billableByDefault`, `budgetKind`, `budgetValue`, `budgetWarnAtPercent`, `customerSnapshot`; `customerId` required on create. Schemas + commands + list projection | `data/validators.ts`, `commands/timesheets-projects.ts`, `api/timesheets/time-projects/route.ts` | T2.2 | **done** — 407 staff tests (+23) | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff` |
| T2.10 | **Regression from T2.9:** `customerId` is now required on project create, so 6 Playwright specs POST without it and will 422 — `TC-STAFF-010/011/012/013/014/023`. Add a customer fixture to each | `__integration__/**` (8 specs + new `fixtures.ts`), `helpers/integration/timesheetFixtures.ts` | T2.9 | **done** — typecheck/lint/unit green; **Playwright run not possible in this env, not claimed** | `yarn test:integration -- TC-STAFF-01` (deferred) |
| T2.11 | **Regression from T2.9:** the edit page still emits `currencyCode` on update, which the schema now strips — so changing the currency of an entry-free project **silently no-ops**. Route it through `POST .../change-currency`, or render the field create-only | `projects/[id]/edit/page.tsx`, `projects/projectFormConfig.ts`, `lib/time-tracking-ui/ProjectCurrencyField.tsx` | T2.9 | **done** — 9 new tests; lint back to 8 warnings | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff` |
| T2.12 | **Audit fidelity:** `assigned_end_date` has no update path (employees route is POST/DELETE only; `staffTimeProjectMemberUpdateSchema` exists but no `time_project_members.update` command does), so T2.5 re-dates an assignment as create-new-then-delete-old. Correct and non-destructive, but the audit trail reads as "removed and re-added" rather than "end date changed" — wrong history for a billing-defensibility system. Add the command + `PUT`; the drawer already emits `endDateUpdates` as a distinct change kind | `commands/timesheets-projects.ts`, `api/timesheets/time-projects/[id]/employees/route.ts`, `lib/time-tracking-ui/ProjectTeamDrawer.tsx` | T2.5 | **done** — 507 staff tests (+22) | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff` |
| T2.8 | Phase gate + integration tests TC-TT-003/004/005/017 | `__integration__/` | T2.1–T2.12 | **done (written, not executed)** — 4 specs / 7 cases. **TC-TT-017 is quarantined with `test.fixme()`** because the guard it tests is not wired — see R3. | `yarn test:integration -- TC-TT-00[345]` |

## Phase 3 — Tasks and Kanban (screens 6, 7)

| Id | Task | Files | Depends | Status | Verify |
|---|---|---|---|---|---|
| T3.1 | Commands + CRUD routes for task statuses (per project, reorder, seed on project create) + **W2 registration** | `commands/timesheets-task-statuses.ts`, `api/timesheets/task-statuses/`, `commands/timesheets-projects.ts`, `lib/timesheets-tasks/` | T1.7 | **done** — 625 staff tests; W2 registered | `yarn test -- task-statuses` |
| T3.2 | Task commands + CRUD route incl. depth-1 guard, parent→child soft-delete cascade, `sequence_number`/`reference` allocation | `commands/timesheets-tasks.ts`, `api/timesheets/tasks/route.ts` | T3.1 | **done** — 60 new tests, 807 total | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff --runInBand` |
| T3.3 | `PATCH .../tasks/[id]/status` with optimistic lock + `clientBroadcast` event | `api/timesheets/tasks/[id]/status/` | T3.2 | **done** — 26 tests, 773 total | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff --runInBand` |
| T3.4a | **Tags** CRUD + task/entry assignment | `commands/timesheets-tags.ts`, `api/timesheets/tags/**` | T1.7 | **done** — 38 tests, 720 total | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff --runInBand` |
| T3.4b | **Task comments** CRUD | `commands/timesheets-task-comments.ts`, `api/timesheets/tasks/[id]/comments/**` | T3.2 | **done** — 29 tests, 890 total | same |
| T3.5 | `rollup.ts` wired into task list responses (`ownMinutes`, `loggedMinutes`, `childCount`) | `api/timesheets/tasks/route.ts`, `data/enrichers.ts` | T3.2, T1.2 | **done** — 27 tests, 748 total | TC-TT-020 |
| T3.6 | `KanbanBoard.tsx` on `@dnd-kit` — columns, counts, hour sums, quick-add, optimistic move + rollback | `lib/time-tracking-ui/KanbanBoard.tsx` | T3.3 | **done** — 23 tests; builds clean | TC-TT-006 |
| T3.7 | `TaskDrawer.tsx` — quick log, subtask checklist w/ own assignee+hours, entries, comments, properties on blur | `lib/time-tracking-ui/TaskDrawer.tsx` | T3.6 | **done** — 33 tests, 1013 total | TC-TT-007 |
| T3.8 | Board filters (assignee, tag, status) with persisted state | `lib/time-tracking-ui/BoardFilters.tsx` | T3.6 | **done** — 13 tests, 1056 total | TC-TT-006 |
| T3.10 | **API gaps the board found.** (a) Card tags render empty — `tags/task-assignments/` has no GET and the tasks projection carries no tag column; needs one enricher field. (b) `doneChildCount` on the rollup enricher, to delete a ≤5-page sweep the drawer's subtask progress currently pays for. (c) no `taskId` filter on the entries list, so the drawer's billable/cost split comes from a bounded 3×100 project sweep instead of an exact cheap query. (d) `staffTimeEntryStartTimerSchema` has no `taskId`, so starting a timer from a card takes two writes and a link failure strands a running project-level timer | `data/enrichers.ts`, `api/timesheets/tags/task-assignments/`, `data/validators.ts`, `api/timesheets/time-entries/start-timer/` | T3.6, T4.2 | **done** — 32 tests, 953 staff | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff --runInBand` |
| T3.9 | Phase gate + TC-TT-006/007/020 | `__integration__/` | T3.1–T3.10 | **done (static)** — 1056 tests, typecheck + lint + builds clean; TC-TT-006/007/020 not yet written | `yarn test:integration -- TC-TT-0(06\|07\|20)` |

## Phase 4 — The time entry model (screens 8, 9, 10)

| Id | Task | Files | Depends | Status | Verify |
|---|---|---|---|---|---|
| T4.1 | Extend time-entry entity write path: `taskId`, `isBillable`, `tagIds`, `rateOverrideAmount`, `roundedMinutes` on write. **MUST also implement `description` request precedence + dual-key (`notes` *and* `description`) response — `UPGRADE_NOTES.md` already promises it (R2 below).** | `commands/timesheets-entries.ts`, `api/timesheets/time-entries/route.ts` | T1.7, T3.2 | **done** — 37 tests; **R2 closed** | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff --runInBand` |
| T4.2 | Lock enforcement in the command layer + `409 entry_locked` across all four write paths. **ALSO: `rounded_minutes` is not written by `start-timer`, `timer-stop` or `bulk`** — they write durations outside the entries command, so those entries cost as `null`/stale. Route them through the same `roundMinutes` call (found by T4.1) | `commands/timesheets-entries.ts`, `api/timesheets/time-entries/bulk/route.ts` | T4.1 | **done** — 31 tests, 891 total; **R3-lock closed** | TC-TT-018 |
| T4.3 | `GET .../time-entries/overlaps` (midnight-aware) | `api/timesheets/time-entries/overlaps/` | T1.2 | **done** — 524 staff tests (+17) | `yarn test -- overlaps` |
| T4.4 | `duplicate` + `copy-day` endpoints. **NOTE: the `duplicate` command already exists** — T4.2 added `staff.timesheets.time_entries.duplicate` (delegates the copy to the create command so it inherits ownership, access, defaults, rounding, tags and audit). Wire a route to it; do **not** write a second command | `api/timesheets/time-entries/[id]/duplicate/`, `.../copy-day/` | T4.1 | **done** — 34 tests | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff --runInBand` |
| T4.5 | `DurationInput.tsx` — parser-backed, preserves invalid text, lists formats, **satisfies W3** | `lib/time-tracking-ui/DurationInput.tsx` | T1.2 | **done** — 576 staff tests (+19) | `yarn test -- DurationInput` |
| T4.6 | `TimeEntryDialog.tsx` — 2-of-3 arithmetic, billable, rate override + live cost, tags, midnight hint, ⌘↵/Esc, save-and-add-another | `lib/time-tracking-ui/TimeEntryDialog.tsx` | T4.5, T4.3 | **done** — 27 tests, 1027 total | TC-TT-008, TC-TT-022 |
| T4.7 | Entries DataTable — inline cell edit, duplicate, delete+undo toast, override badge, locked rows read-only | `backend/staff/time-tracking/entries/page.tsx` | T4.2, T4.4 | **done** — 19 tests, 1067 total | TC-TT-010 |
| T4.9 | **Silent no-op found by T4.6:** `staffTimeEntryCreateSchema` accepts `startedAt`/`endedAt` but `staffTimeEntryUpdateSchema` does not, and the update command never assigns them — so **editing an existing entry's start/end reports success and changes nothing**. Same class as the currency bug T2.11 fixed. The dialog already sends the keys | `data/validators.ts`, `commands/timesheets-entries.ts`, `api/timesheets/time-entries/route.ts` | T4.6 | **done** — 21 tests, 1048 total | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff --runInBand` |
| T4.10 | **Adjacent defect found by T4.9:** `bulk/route.ts` writes `durationMinutes` + `roundedMinutes` onto an existing entry **without touching its clocks**, so bulk-editing the duration of an entry that has a start/end leaves exactly the contradictory triple T4.9 just fixed elsewhere. Also: changing only `date` does not re-anchor the clocks onto the new day (an API caller sending `date` alone leaves `started_at` on the old day; the dialog is unaffected because it rebuilds them) | `api/timesheets/time-entries/bulk/route.ts`, `commands/timesheets-entries.ts` | T4.9 | **done** — 21 tests, 1088 | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff --runInBand` |
| T4.8 | Phase gate + TC-TT-008/009/010/018/022 | `__integration__/` | T4.1–T4.10 | **done (static)** — 1178 tests, typecheck + lint + builds clean; TC-TT-008/009/010/018/022 not yet written | `yarn test:integration -- TC-TT-0(08\|09\|10\|18\|22)` |

## Phase 5 — Timesheet and My work (screens 1, 2, 11, 12)

| Id | Task | Files | Depends | Status | Verify |
|---|---|---|---|---|---|
| T5.1 | Period selector (year/quarter/month/week) preserving date context + persisted filters | `lib/time-tracking-ui/{timesheetPeriod.ts,PeriodSelector.tsx,useTimesheetPreferences.ts}` | T4.8 | **done** — 30 tests | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff --runInBand` |
| T5.2 | `TimesheetCalendar.tsx` — month grid, day totals, load bars vs `targets.dailyHours`, non-billable dashed, hover add | `lib/time-tracking-ui/{TimesheetCalendar.tsx,timesheetData.ts,TimesheetPeriodFooter.tsx}` | T5.1 | **done** — 22 tests | same |
| T5.3 | Week list view — day bars, expand deviating day, per-day table, quick-add row | `lib/timesheets-ui/ListView.tsx` | T5.1 | **done** — 16 tests (12 new + the 4 inline-description regressions ported) | same |
| T5.4 | **Keep + upgrade the editable grid**: shared duration parser, project-or-task rows, locked cells read-only | `backend/staff/time-tracking/timesheet/GridView.tsx` | T5.1, T4.2 | **done** — 14 tests; **found R4** (bulk drops `taskId`) | TC-STAFF-027 (static analysis only — no Playwright env) |
| T5.5 | Three-way view switch, persisted; grid default for week, calendar for month | `backend/staff/time-tracking/timesheet/page.tsx` | T5.2, T5.3, T5.4 | **done** — grid offered for week+month only (see log) | same |
| T5.6 | `GET .../my-work` aggregate + My work dashboard (screen 1) + empty state (screen 2) | `api/timesheets/my-work/`, `backend/staff/time-tracking/page.tsx` | T4.8 | **done** — 7 tests | same |
| T5.7 | Phase gate + TC-TT-001/002/011/012 + TC-STAFF-027 regression | `__integration__/` | T5.1–T5.6 | **done (static)** — 1174 staff tests (1173 pass + 1 pre-existing TZ flake, see E6), typecheck + lint + builds clean; TC-TT-001/002/011/012 not yet written | `yarn test:integration -- TC-TT-0(01\|02\|11\|12)` |

## Phase 6 — Reports and billing (screens 13, 14, 15)

| Id | Task | Files | Depends | Status | Verify |
|---|---|---|---|---|---|
| T6.1 | Report commands + CRUD + `reference` allocation | `commands/timesheets-reports.ts`, `api/timesheets/reports/route.ts` | T4.8 | **done** — 16 command tests; W2 registered; W1 closed (Zod 4 keeps `.superRefine()` a `ZodObject`) | `cd packages/core && npx jest --config jest.config.cjs src/modules/staff --runInBand` |
| T6.2 | `POST .../reports/preview` — totals without persisting, single-currency assert, already-reported detection | `api/timesheets/reports/preview/` | T6.1, T1.2 | **done** — 24 `reportTotals` tests incl. the D-7 invariance proof | TC-TT-021 (not written) |
| T6.3 | Report config screen — presets filling editable range, project pick-list w/ live numbers, already-reported panel + opt-in | `backend/staff/time-tracking/reports/create/page.tsx` | T6.2 | **done** — D-4 reducer + 11 tests | TC-TT-013 (not written) |
| T6.4 | `ReportSheet.tsx` preview — project groups, rolled-up task lines, override badges, non-billable group, grand total | `lib/time-tracking-ui/ReportSheet.tsx` | T6.2 | **done** — plus `GET .../reports/[id]/sheet` and the detail page | TC-TT-014 (not written) |
| T6.5 | Close & lock — snapshot writer, entry lock, frozen totals, `closed` event, `withAtomicFlush` | `commands/timesheets-reports.ts`, `api/timesheets/reports/[id]/close/` | T6.1, T4.2 | **done** — one transaction; no write path touched | TC-TT-015 (not written) |
| T6.6 | Unlock — reason required, history timeline, destructive-outline | `api/timesheets/reports/[id]/unlock/`, `backend/.../reports/[id]/page.tsx` | T6.5 | **done** — gated on `reports.unlock`, freeze records removed, reason in the event | TC-TT-015 (not written) |
| T6.7 | Exports — PDF (sheet), CSV/XLSX (raw columns), `exported` event | `api/timesheets/reports/[id]/export/` | T6.4 | **done** — dependency-free PDF + XLSX writers; export never locks | TC-TT-014 (not written) |
| T6.8 | Phase gate + TC-TT-013/014/015/021 | `__integration__/` | T6.1–T6.7 | **done (static)** — 1295 staff tests / 125 suites, typecheck + lint + builds clean; TC-TT-013/014/015/021 not yet written | `yarn test:integration -- TC-TT-0(13\|14\|15\|21)` |

## Phase 7 — Settings and polish (screen 16)

| Id | Task | Files | Depends | Status | Verify |
|---|---|---|---|---|---|
| T7.1 | Settings page — rounding unit/direction w/ live examples, entry defaults, daily target, warning toggles | `backend/staff/time-tracking/settings/page.tsx` | T6.8 | todo | TC-TT-016 |
| T7.2 | `GET .../settings/rounding-impact` — 90-day preview | `api/timesheets/settings/rounding-impact/` | T7.1 | todo | `yarn test -- rounding-impact` |
| T7.3 | Retro-rounding as an explicit ProgressJob worker; never touches locked entries | `workers/timesheets-reapply-rounding.ts`, `api/timesheets/settings/reapply-rounding/` | T7.2 | todo | TC-TT-016 |
| T7.4 | Keyboard-first pass across the entry loop | `lib/time-tracking-ui/**` | T6.8 | todo | manual + TC-TT-008 |
| T7.5 | i18n completion — `staff.time_tracking.*` across 5 locales | `i18n/*.json` | T7.1 | todo | `yarn i18n:check-hardcoded && yarn i18n:check-values` |
| T7.6 | DS sweep — semantic status tokens; migrate the grid's green pill + amber dirty-cell | `backend/staff/time-tracking/**` | T7.1 | todo | `yarn lint` (ds config) |
| T7.7 | `staff/AGENTS.md` — document new public surfaces | `AGENTS.md` | T7.6 | todo | `yarn agents:check-budget` |
| T7.8 | Final gate — full CI-mirroring sequence | — | T7.1–T7.7 | todo | `yarn generate && yarn build:packages && yarn typecheck && yarn lint && yarn test && yarn build:app` |

---

## Decisions in force (from the spec)

D-1 per-project Kanban columns · D-2 subtasks are child tasks carrying time, parent shows inclusive rollup · D-3 currency read-only once entries exist · D-4 presets fill an editable range · D-5 already-reported entries excluded by default · D-6 access request is a real notification · D-7 round at the entry then sum upward · D-8 end-before-start reads as midnight crossing · D-9 `customer_id` → `customer_entities` + snapshot · D-10 project code auto-derived, 20-char cap.

## Standing constraints for every task

- Tenant + organization scoping on every query; never cross-tenant.
- Writes go through commands, not raw ORM in route handlers.
- `withAtomicFlush` when a phase queries between mutation and flush; side effects and cache invalidation **after** the commit.
- Custom write routes wire the mutation guard registry.
- Client code uses `apiCall`/`apiCallOrThrow`; non-`CrudForm` writes use `useGuardedMutation`.
- No hardcoded user-facing strings; no raw Tailwind status colors; no arbitrary values.
- `yarn db:generate` may emit unrelated migrations — drop those from the diff, keep only `staff`, update its snapshot.
- Never run `yarn db:migrate`.

## Rework and watch items

**R4 — `= ANY(?)` in raw SQL 500s every non-empty project list.** *(CLOSED 2026-08-16)*

Reported as a 500 on `/api/staff/timesheets/time-projects`. It reproduced only once a project row existed, which is why an empty demo database looked healthy and why no unit test caught it: **the test mocked `execute` and asserted the array parameter the broken code passed.**

MikroORM does not bind parameters at the driver level — `AbstractSqlConnection.execute` calls `platform.formatQuery`, which interpolates each value through `BasePostgreSqlPlatform.escape`, and that renders a JS array as a bare comma-separated list. `column = ANY(?)` therefore reaches PostgreSQL as `= ANY('a','b')`, or for a single-element array as the bare scalar — `malformed array literal: "<uuid>"`. The dashboards aggregation builder hit exactly this in #4669 and settled on one placeholder per member; `computeProjectFinancials`, `computeProjectHoursTrend`, `computeTaskRollups` and the task-tag enricher were already written that way.

Three sites were not, all mine, all from Phases 5–7:

| Site | Symptom |
|---|---|
| `api/timesheets/time-projects/route.ts` → `decorateProjectCurrencyLock` | **every** project list request 500s as soon as one project exists |
| `api/timesheets/settings/rounding-impact/route.ts` | the 90-day preview 500s whenever the caller is org-narrowed (this is the `staff.timesheets.settings.rounding-impact failed` line already sitting in the dev log) |
| `lib/time-tracking/reapplyRounding.ts` → `organizationClause` | the retro-rounding job counts and pages zero candidates, so it would have reported success having rewritten nothing |

Fixed by a shared `lib/time-tracking/sqlInClause.ts`; an empty list renders `IN (NULL)` so a caller that narrowed to nothing selects nothing rather than silently widening. The two tests that asserted `ANY(?)` were rewritten to assert the placeholder form **and** that no bound parameter is an array — the assertion that would have caught this originally.

**Lesson recorded:** a unit test whose fake accepts any argument shape verifies the caller against itself, not against the database. Where a helper builds SQL for a real driver, assert the *binding shape* the driver requires, not just the values.

**T8.1 — demo data.** *(DONE 2026-08-16)*

`lib/timeTrackingSeeds.ts` + `mercato staff seed-time-tracking-examples`, wired into `setup.ts seedExamples` after the team examples (which create the staff members it staffs projects with). Registered *after* `customers` in the module registry, so a fresh `--with-examples` init has real `customer_entities` to link to; a tenant without them degrades to a snapshot name and a null `customer_id` rather than skipping the project.

4 projects (one `on_hold`, with history that stops 21 days ago), 16 Kanban columns from the default template, 27 tasks incl. subtasks, 5 comments, 5 tags, ~450 entries over 10 weeks of weekdays chained from 09:00 so no day overlaps itself, and 2 reports — last month **closed** (frozen entries + locked originals, written with the same `entryAmount`/`round2`/`sumAmounts` the close command uses) and this month draft. All of it from a fixed LCG, so a re-seed reproduces the same hours and the same totals.

**R3 — project access was not enforced anywhere.** *(CLOSED 2026-08-13)*

The spec's "Project access is one resolver" says every board, task, entry, timesheet and report route filters through `resolveProjectAccess`. Verified against the tree:

- `grep -rn NoProjectAccess` across `packages/core/src` and `apps` finds **no importer**. T2.6 built, translated and unit-tested the component; nothing renders it.
- Only **one** route under `api/timesheets/` consults `timeTrackingAccessResolver` — `time-entries/overlaps/` (T4.3, landed today). `GET /api/staff/timesheets/time-projects` does not.
- That route is gated on `staff.timesheets.projects.view`, which `setup.ts` grants to **`employee`**. So a Team Member who is not a project member currently **receives the project record** — customer name and project name leak outright.

This breaks **US-A1** ("unassigned/other customers are not visible"), **US-A2**, and screen 17 note 1 (the message must not confirm a resource exists). The detail page's only negative branch is `RecordNotFoundState` — no request-access action, wrong message.

**I marked T2.6 `done` on its unit tests. That was wrong** — the component was verified in isolation and never checked for a caller. Corrected to PARTIAL.

**Fixed:** `api/timesheets/time-projects/route.ts` now resolves access and narrows list **and** `?id=` detail; `projects/[id]/page.tsx` and `[id]/edit/page.tsx` both render `NoProjectAccess` instead of `RecordNotFoundState`; `TC-TT-017` is un-quarantined and lists as a real test. Staff suite **625 passing**, core+app typecheck clean, eslint at baseline.

**Policy question CLOSED 2026-08-13 → spec D-13:** keep the `projects.view` grant, scope the data. A Team Member keeps the Projects nav entry and sees only the projects they are assigned to. **No code change needed** — the R3 fix already narrows the list through `resolveProjectAccess`.



**R1 — `em.create()` regression from the new entity columns.** *(CLOSED 2026-08-13)*
New defaulted columns were declared as non-optional TS properties with initialisers (`isBillable: boolean = true`). MikroORM's `RequiredEntityData` still treats those as required, so six existing call sites fail `TS2345` — `commands/timesheets-entries.ts` ×3, `commands/timesheets-projects.ts` ×2, `api/timesheets/time-entries/bulk/route.ts` ×1. Fix is an `[OptionalProps]` declaration on the entities (precedent: `customers/data/entities.ts`), **not** edits to the call sites, and the DB keeps `NOT NULL DEFAULT`.

Fixed by `[OptionalProps]` on 12 entities; migration and snapshot MD5s byte-identical before and after, confirming a TypeScript-surface-only change.

**Lesson 1: `yarn workspace @open-mercato/core build` does not typecheck** — it is esbuild/tsup, so it passes on code that does not compile. Every entity/command task must gate on a real typecheck. T1.1's Verify column has been corrected.

**Lesson 2: this repo typechecks with two different compilers.** `packages/*` run JS TypeScript 6 (`tsc --noEmit`); `apps/mercato` runs **native TS 7 (`typescript-native`/tsgo)** via `apps/mercato/scripts/typecheck.mjs`, pinned that way during the TS 7 migration. A package passing does **not** imply the app passes. Always gate on `yarn typecheck` (both), not on a single `--filter`.

**Lesson 3 — my own error, recorded so it is not repeated.** After the R1 fix, `@open-mercato/core` passed while `@open-mercato/app` still reported the six `TS2345`. I hypothesised that tsgo does not honour the `[OptionalProps]` computed-symbol marker and probed a `Opt<T>` conversion, which appeared to fix it. **The hypothesis was wrong.** The control experiment — reverting the probe entirely — showed the app still clean, so the residual errors had been **stale incremental compiler state**, not a compiler difference. `[OptionalProps]` works correctly under both compilers and the probe was reverted (`entities.ts` byte-identical to T1.1's output). Two takeaways: run the control before accepting a fix, and re-run a gate from a clean state before diagnosing it. A first probe was additionally run from the repo root, where the root `tsconfig.json` has empty `include` — it reported "0 errors" because it compiled **nothing**. A gate that passes by checking nothing is worse than one that fails.

**W1 — report schemas are `ZodEffects`, not `ZodObject`.** *(CLOSED 2026-08-15 by T6.1 — the premise does not hold on this repo's Zod.)*
T1.3 added a `periodTo >= periodFrom` `superRefine` to the report create/update/preview schemas. The concern was Zod 3 semantics, where `.superRefine()` wraps the schema in a `ZodEffects` and loses `.shape` / `.extend` / `.partial`. **This repo runs Zod 4.4.3, where `.superRefine()` returns the same `ZodObject` with the check attached** — verified directly: `schema instanceof z.ZodObject` is `true`, `.shape` and `.extend()` work, and an extended schema still enforces the refinement. So `withIdsQueryParam`'s `instanceof z.ZodObject` guard passes, `parseScopedCommandInput` (generic over `z.ZodTypeAny`) is unaffected, and `createCrudOpenApiFactory` only stores the schema. Precedent confirmed in the tree: `staffLeaveRequestCreateSchema` is the same shape and already ships through both `parseScopedCommandInput` and `createStaffCrudOpenApi` at `api/leave-requests.ts:274,397`. **No adaptation needed; the refinement is kept.**

**W6 — `currency_code` case normalization.** *(CLOSED 2026-08-13)* T2.3 stores exactly what the validator yields (trimmed, length 3) rather than upper-casing, deliberately declining to add an undocumented transform no other writer shares. But the report single-currency assertion (risk R2) compares currency codes **case-sensitively**, so `pln` and `PLN` would read as two different currencies and wrongly block a report. Nothing writes `currency_code` yet, so the fix is free right now: add `.toUpperCase()` to `currencyCodeSchema` in `data/validators.ts` (T1.3's file). **Fixed:** `currencyCodeSchema` in `data/validators.ts` now ends `.transform(v => v.toUpperCase())`, so every writer stores canonical ISO 4217 at the boundary. Staff suite still 305/305. Closed before T2.2 — the first writer — could bake in mixed case.

**R2 — `UPGRADE_NOTES.md` documented behaviour that was only half-implemented.** *(CLOSED 2026-08-14 by T4.1 — both keys accepted on write with `description` winning, both returned on read.)*
The release note promises that `staff_time_entries.notes` is also exposed as `description`, with both keys returned for ≥1 minor release. Reality: `data/validators.ts` accepts `description` on create/update (Phase 1), but `api/timesheets/time-entries/route.ts` neither maps it into the field map nor returns it, and `commands/timesheets-entries.ts` only ever reads `parsed.notes`. **T4.1 must implement request precedence and the dual-key response, or the published note is false.** Caught by T2.1 while writing the note.

**W7 — sidebar nesting.** *(CLOSED 2026-08-13 → spec D-11: accept the platform's nesting. No work.)*
`buildAdminNav` (`packages/ui/src/backend/utils/nav.ts:344-367`) builds hierarchy by href prefix inside a group, and `/backend/staff/time-tracking` is a prefix of the other six routes. So the sidebar renders **My work** with the other six indented beneath it, expanding only when the pathname is inside the module. The existing `Employees` group already behaves this way (Projects nested under My Timesheets), so this is platform behaviour, not a metadata error — but the mockups (every `<section class="screen">` sidebar) draw seven flat siblings. Flattening needs a nav opt-out in `packages/ui`, which is an "ask first" nav-contract change.

**W8 — two D-12 loose ends left deliberately out of T1.8's scope.**
(a) `api/timesheets/settings/route.ts` declares a docs-only `settingsResponseSchema` for OpenAPI that now omits the new `access` group — add `access: z.object({ assignmentGraceDays: z.number() })`. Documentation-only; no runtime or test impact. (b) `staff/AGENTS.md` documents the `resolveProjectAccess` context shape and does not yet mention `assignmentGraceDays` / `now`. Fold both into T7.1 (settings page) and T7.7 (AGENTS.md) respectively.

**W9 — the board's tag filter is client-side and bounded by pagination.** *(open, low)*
`api/timesheets/tasks` has no `tagIds` parameter, so T3.8 filters loaded rows instead. It is honest about the bound rather than hiding it, but exact cross-page tag filtering needs a `tagIds` filter on the route (a `buildTaskListFilters` addition plus a join on `staff_time_task_tags`). Cheap; worth doing when someone is next in that file.

**W10 — board filter persistence is per browser profile, not per identity.** *(open, low, deliberate)*
`useBoardFilters` threads a `userKey` that no caller passes, matching `useProjectsViewMode`'s existing behaviour. Keying on the staff member id would be strictly more correct but introduces a null→id transition on first paint that either flashes an unfiltered board or writes to the wrong key. T3.8 chose the existing precedent over inventing a second storage idiom — reasonable, and reversible.

**W5 — three i18n keys referenced but not defined.** The settings route renders `staff.errors.forbidden`, `staff.errors.invalid_request` and `staff.errors.internal`, none of which exist in `staff/i18n/*.json` (only `staff.errors.unauthorized` does). They fall back to English today. Add across `en/pl/de/es/ko` in T7.5.

**W4 — expired project assignments.** *(CLOSED 2026-08-13 → spec D-12: enforce the window with a 14-day configurable grace. Implemented by new task T1.8.)*
`staff_time_project_members` has `assigned_start_date` (required) and `assigned_end_date` (nullable), but the spec's access rule is only "active, non-deleted" — it never mentions the date window. T1.6 implemented the spec literally, so **a member whose `assigned_end_date` is in the past still has full access** to the project, its tasks and its entries. That may well be wrong for a consultancy rotating people off engagements. The resolver is the single chokepoint, so this is a one-place change whichever way it goes. Decide before Phase 2 ships the team drawer (T2.5), which is where end dates become settable in the UI.

**W3 — decimal formatting vs the grid's look.** *(CLOSED 2026-08-13 by T4.5)* `90 → "1.50"`, `120 → "2.00"`. The existing grid's private `minutesToDecimal` strips trailing zeros (`120 → "2"`), which is the look users have today. **Closed:** `DurationInput` exports `formatTrimmedDecimalDuration`, reproducing the grid's private `minutesToDecimal` byte for byte (`120 → "2"`, `90 → "1.5"`, `100 → "1.67"`, `0 → ""`). T5.4 mounts it as `formatValue={formatTrimmedDecimalDuration}` and the grid renders exactly as today. A test asserts `formatDuration(120,'decimal') === '2.00'` beside `formatTrimmedDecimalDuration(120) === '2'`, so the divergence is documented and drift fails.

**W2 — new editable entities are not registered with the optimistic-lock guard.** `packages/core/src/__tests__/optimistic-lock-editable-entities.test.ts` curates its list by hand. `StaffTimeTaskStatus`, `StaffTimeTask`, `StaffTimeTaskComment`, `StaffTimeTag` and `StaffTimeReport` all carry `updated_at` correctly, but the guard is not watching them, so a later regression would pass silently. Register them in the task that owns their CRUD routes (T3.1/T3.2/T3.4, T6.1).

**R4 — `POST .../time-entries/bulk` silently drops `taskId` (and every other consulting-suite field).** *(open, found by T5.4, NOT fixed — outside Phase 5's scope)*

`staffTimeEntryBulkItemSchema` accepts `taskId`, `description`, `isBillable`, `tagIds` and `rateOverrideAmount`, but `api/timesheets/time-entries/bulk/route.ts` reads none of them: the create branch calls `trx.create(StaffTimeEntry, { … })` without `taskId`/`isBillable`/`rateOverrideAmount`, and the update branch assigns `date`, `timeProjectId`, the interval, `roundedMinutes` and `notes` only. A caller sending `taskId` gets `200 OK` and a task-less entry — the fifth silent no-op of this build (cf. T2.11, T4.9, T4.10).

Consequence for T5.4: the grid's task rows are **not** routed through bulk. Project rows keep the bulk path byte-for-byte (so `TC-STAFF-027` is untouched); a task-row cell is written through the single-entry CRUD route, which does honour `taskId` along with locks, rounding, access, audit and undo. The workaround is one function (`saveTaskCells` in `GridView.tsx`) and collapses into the batch the moment bulk learns the field. Fixing bulk is ~4 lines and should be scheduled.

**E6 — `TaskDrawer.test.tsx` fails between 00:00 and 02:00 local time.** *(pre-existing, environmental)*

`TaskDrawer` composes "today" in LOCAL time (`todayIsoDate()`), the test compares against `new Date().toISOString().slice(0, 10)` — UTC. In a UTC+2 zone the two disagree for the first two hours of every day, so `logs time from the single field with today / me / billable defaults` fails with `Expected "2026-08-14" / Received "2026-08-15"`. `TZ=UTC npx jest …` passes 15/15. Not caused by Phase 5 and not fixed here (the component is out of scope); the test's expectation should be built with the same local-time helper the component uses.

## Environment blockers

**E1 — `node_modules` is stale, predating this work.** The last install was 30 Jul 23:51; `yarn.lock` was last modified 12 Aug 22:10. Dependencies moved in the lockfile and were never installed, so at baseline the repo does not typecheck and `turbo` is not on `PATH` in a non-interactive shell.

Confirmed missing or wrong-version:
- `@tanstack/react-table` — installed build has no `legacy` export path, so `packages/ui` fails to resolve `@tanstack/react-table/legacy`
- `bullmq-otel` — absent entirely (`packages/queue`)
- parts of `@opentelemetry/*` — partial tree (`packages/telemetry`)

**Consequence:** `yarn typecheck`, `yarn lint`, `yarn test` and `yarn build:*` cannot be trusted as pass/fail gates until `yarn install` is run. Task verification is therefore deferred, not skipped — nothing is marked `done` on an unverified gate.

**RESOLVED 2026-08-13** by `yarn install --immutable` (exit 0, lockfile untouched), followed by `yarn build:packages` and `yarn generate` — both of which the test pipeline needs and neither of which turbo runs as a test dependency.

### Verified baseline (2026-08-13, with all Phase 1 work in the tree)

| Gate | Result |
|---|---|
| `yarn install --immutable` | exit 0, `yarn.lock` unmodified |
| `yarn build:packages` | 22/22 |
| `yarn generate` | pass |
| `yarn typecheck` | 20/22 — **one known failure**, see below |
| `yarn test` | **29/29 successful** |
| `yarn lint` | pass — 0 errors, 10 warnings (pre-existing, `@open-mercato/app`) |

**The one known typecheck failure is not ours:** `@open-mercato/enterprise#typecheck`, from `src/modules/agent_orchestrator/generated/file-agents.generated.ts` importing `../lib/sdk/outcomeSchema.js` and `../lib/tokens/types.js`, neither of which exists. That whole directory is **untracked** (`?? packages/enterprise/src/modules/agent_orchestrator/`) — unrelated in-progress work that predates this build. Later gates compare against this baseline, not against zero.

**E2 — intermittent Yarn 4 binary-resolution flake under turbo.** Roughly one run in two, a workspace's test task dies instantly with `command not found: jest` (exit 127); the same has been seen as `command not found: turbo` in a non-interactive shell. It hits a different package each time and **passes on re-run with no change**. Once it also surfaced as `Failed: @open-mercato/ui#test` while that package's own output read `229 passed, 1861 tests passed` — jest succeeded and the wrapper exited non-zero. Standalone `cd packages/ui && yarn test` → exit 0.

**Do not chase this as a code defect.** Confirm by running the package directly (`cd packages/<x> && yarn test`); if that is exit 0, the turbo failure is the flake. Re-run the gate before reporting any failure from it.

**E3 — `yarn lint` does not lint `packages/*` at all.** Only `apps/mercato` declares a `lint` script (`eslint .`); no package does. So the "0 errors" baseline covers the app and nothing else, and **every line of this feature lives in `packages/core`**. T2.4 found 2 real errors this way — `@next/next/no-html-link-for-pages` on the timer sidebar widget, invisible to `yarn lint` twice over: once because packages are not linted, and again because the Next rule only resolves when eslint runs **from `apps/mercato`** (from the repo root it prints "Pages directory cannot be found" and silently skips). Both fixed.

**Lint the module explicitly, from the app directory:**
```
cd apps/mercato && npx eslint ../../packages/core/src/modules/staff --ext .ts,.tsx
```
Current: **0 errors, 8 warnings** (all pre-existing `react-hooks/exhaustive-deps` + one `import/no-anonymous-default-export`).

**E5 — the host machine sleeps mid-run, killing background agents.** *(open, blocking)*
Six terminations with `API Error: Your computer went to sleep mid-response` across four dispatches. T3.1 was killed **four times**; T4.3 twice; T4.5 once (it completed after a resume). Each kill discards the agent's in-context reading, so a resume pays for the spec and mockup reads again — the tasks are 100k–280k tokens each, mostly research.

**No work has ever been lost or corrupted by this** — every kill was audited against the tree before resuming, and the staff suite has stayed green at each checkpoint (currently **524 passing**). It is purely a throughput problem, but a severe one: T3.1 has consumed four full dispatches and produced one 132-line helper.

**Fix (user action):** `caffeinate -dimsu` in a spare terminal, or System Settings → Displays → *Prevent automatic sleeping on power adapter*. Until then, background agents cannot be relied on to finish.

**Mitigation applied:** resume prompts now instruct agents to write each file as it is finished rather than batching to the end, since anything on disk survives a kill and anything in context does not.

**E4 — jest worker processes segfault under `turbo run test`.** *(open, unattributed)*

Symptom: `@open-mercato/cli` and `@open-mercato/ai-assistant` fail intermittently with

```
● Test suite failed to run
  A jest worker process (pid=…) was terminated by another process:
  signal=SIGSEGV, exitCode=null
```

Established by experiment:
- **No assertion ever fails.** `--runInBand` passes 100%: cli **87 suites / 1679 tests**, ai-assistant **106 suites / 1454 tests**.
- A **different suite dies each run** (`module-facts.local-reference`, `module-facts.owned-contracts`, …), and each passes alone. The reported test count varies run to run because whichever suite is killed drops its tests from the total.
- Not fixed by `yarn rebuild` (all 10 native packages rebuilt: `better-sqlite3`, `isolated-vm`, `msgpackr-extract`, `cpu-features`, `ssh2`, …).
- Not reliably fixed by heap: still ~1-in-3 at `--max-old-space-size=3072`, versus the root `test` script's pinned **1024**.
- `maxWorkers: 2` and `workerIdleMemoryLimit: 512MB` are already pinned in `jest.config.base.cjs` for exactly this class of problem (issue #2402); dropping to `--maxWorkers=2` explicitly did not help.

**Attribution is unresolved and I am not claiming otherwise.** The dying suites are the `module-facts.*` generator tests, which scan every module in the repo, and this work adds substantial module surface for them to scan — so a causal link is plausible. Against that: `yarn test` passed **29/29 twice** at the T1.7 gate, after Phase 1 had already added ~40 files. Proving it needs a clean-checkout comparison, which was not attempted because `git stash` across 60+ files including staged renames risks the work (a subagent already hit an index conflict doing exactly that).

**Workaround for a trustworthy signal:** `npx jest --config jest.config.cjs --runInBand` per package.
**Watch:** CI runs plain `yarn test`, so this will show up as flaky red on unrelated PRs.

**Setup order matters and is not enforced by turbo:** `install → build:packages → generate → test`. Skipping `build:packages` fails `@open-mercato/telemetry` on an unresolved `@open-mercato/shared/lib/telemetry/runtime`; skipping `generate` fails 7 `@open-mercato/app` suites on a missing `modules.i18n.loaders.generated`. Neither failure is a real defect.

## Progress log

| When | Event |
|---|---|
| 2026-08-16 | **R4 closed and T8.1 done.** The 500 on the projects list reproduced the moment a single project row existed — it was never a data problem and never the access resolver I had been reading. Three raw-SQL sites bound a JS array to one `= ANY(?)` placeholder, which MikroORM interpolates rather than binds; the projects list was dead for every non-empty tenant, the rounding-impact preview was dead for any org-narrowed caller, and the retro-rounding job would have reported success having selected nothing. Both existing tests asserted the broken shape, because their `execute` fakes accept any argument. Fixed behind one shared `buildSqlInClause`; the tests now pin the placeholder form and that **no bound parameter is an array**. Also **found the reason my curl login kept failing**: `/api/auth/login` parses only form-encoded and `formData()` bodies, never `application/json`, so a JSON body falls into the catch, parses as empty strings and returns 400 — my tooling, not the app. Then landed the demo seed (T8.1): 4 projects, 27 tasks, ~450 entries over 10 weeks, one closed report locking 122 entries. Staff suite **1385/1385**, core typecheck clean, eslint at baseline. |
| 2026-08-15 | **PHASE 6 COMPLETE (static gates).** T6.1–T6.8. Gate: **1295 staff tests / 125 suites** (baseline 1174 → **+121 tests, +16 suites**, none removed), core+app typecheck clean, eslint **0 errors / 8 warnings** (baseline), `build:packages` 22/22, `build:app` clean, locales **1836 keys × 5 in parity** (+140, zero deletions). **W1 closed** — the ZodEffects concern is a Zod 3 assumption; on Zod 4.4.3 `.superRefine()` returns a `ZodObject`, so nothing needed adapting and the `periodTo >= periodFrom` refinement is kept. **D-7 is proved, not asserted**: `computeReportTotals` sums over ENTRIES and never over lines, so the grand total is byte-identical across task / person / day groupings while the line keys demonstrably differ — and a test pins the divergence the rule chose against (three 665-minute entries at 320/h bill 10 640.01 entry-rounded versus 10 640.00 sum-then-rounded). **D-5 is enforced in one predicate** (`isAlreadyReported`) shared by the preview, the sheet and the freeze writer, so the panel's count, the excluded lines and the frozen set can never disagree; the opt-in re-admits an hour at its FROZEN values and **does not steal the earlier report's lock** — ownership stays with the report that froze it first, or unlocking the earlier one would leave an hour editable while a later closed report still quotes it. **R2 blocks before any amount exists**, naming both currencies and the offending projects. Close is one `withAtomicFlush({ transaction: true })` and touches **no** entry write path — T4.2's command-layer lock already covers them. Unlock **deletes** its freeze records (so those hours stop counting as already reported, and a re-close cannot hit the unique index) and copies the frozen totals into the `unlocked` event's metadata, keeping the audit. Export ships **dependency-free PDF and XLSX writers** (`node:zlib` for the ZIP; WinAnsi + a `/Differences` array so Polish diacritics survive a client-facing PDF) rather than an "ask first" production dependency — and **never locks**. Found one out-of-scope defect: the entries list route has no `locked` / `lockedReportId` filter although the spec's API Contracts promises one; screen 15's locked-entry table is served from the report sheet endpoint instead. |
| 2026-08-15 | **PHASE 5 COMPLETE (static gates).** T5.1–T5.7. Gate: **1174 staff tests** (1173 pass; the one failure is E6, a pre-existing timezone-dependent expectation in `TaskDrawer.test.tsx` that passes under `TZ=UTC`), core+app typecheck clean, eslint **0 errors / 8 warnings** (baseline), `build:packages` 22/22, `build:app` clean, locales **1695 keys × 5 in parity** (+71). The measured HEAD baseline for `npx jest src/modules/staff --runInBand` is **1088 / 109 suites** (the "1178" recorded at the T4.8 gate is not reproducible with that command), so Phase 5 adds **+86 tests / +8 suites** and removes none — verified against a throwaway git worktree at HEAD rather than asserted. **The grid is kept, not rebuilt**: its table markup, column widths, weekend treatment, totals and footer are carried over; the three upgrades are the shared `DurationInput` mounted with `formatValue={formatTrimmedDecimalDuration}` (so `120 → "2"`, W3), a row-mode toggle that splits a project into its task-less row plus one row per task with time, and locked cells that render read-only with a lock badge and absorb the bulk endpoint's `409 lockedEntryIds` per cell. Unparseable cell text is now **kept** and blocks Save instead of being stripped character by character. The four legacy summary cards (Week total / Working days / Daily average / the hardcoded green `Status: Open` pill) were replaced by the mockups' period footer — page chrome, not the grid. The view switch offers the grid for **week and month only**: ninety day columns is not a bulk-entry surface, so the option is absent rather than disabled, and a remembered grid choice falls back to the calendar on a quarter or a year. Somebody else's timesheet is **read-only** in every view, because every write path in the module resolves the caller's own staff member — an editing affordance there could not have worked. Raised **R4** (bulk drops `taskId`) and **E6**. |
| 2026-08-14 | **PHASE 4 COMPLETE (static gates).** T4.10 done. Gate: **1178 tests**, core+app typecheck clean, eslint 0 errors, `build:packages` 22/22, `build:app` clean. T4.10 imported the reconciliation helper rather than routing bulk through the command, because the bulk row update runs inside `em.transactional` on entities loaded in that transaction while the command forks its own EM and emits side effects per row — routing through it would have broken the single-transaction guarantee and re-resolved access up to 200 times. Its `date`-move shifts clocks by **whole days** rather than recomposing timestamps, which preserves both a midnight crossing and the stored offset (a tenant east of UTC legitimately has a start on the previous UTC day) — and it **skips a running timer entirely**, since re-anchoring the start of work still in progress would restate its elapsed time. A pure date move does not re-round: "the day changed, not the length." |
| 2026-08-14 | **T4.7 done** — the entries list, 19 tests, 1067 total. Undo-delete reuses the platform's `LastOperationBanner` and the command's **own** `undo` via the audit-log action, so the row returns **from the server** with tags, rounding and audit intact rather than being rebuilt from client state — and it cited the `.ai/ui-backend-components.md` rule forbidding a second banner. Escape reverts an inline edit by remounting `DurationInput` with a fresh `key`, because that field deliberately owns the typed text and "a key bump is the only honest way to discard it from outside". `summarizeTimeEntries` returns **one subtotal per currency and never a pre-added figure**, so the renderer cannot be handed a cross-currency sum — the wrong thing is unrepresentable rather than merely undone. It declined to ship the description search: `staff/search.ts` has no `staff_time_entry` config, so a `q` param would silently empty the table, and **"shipping a search box that silently empties the table is worse than not shipping one."** |
| 2026-08-14 | **Spec corrected:** the lock error code is `time_entry_locked`, not `entry_locked` — the spec said the latter in two places (API Contracts and risk R3) and I repeated it in two task briefs. The implementation has used `time_entry_locked` since T3.4a, deliberately sharing one code with the tag commands. T4.7 accepted both defensively and flagged the mismatch. |
| 2026-08-14 | **T4.9 done** — the silent no-op is fixed, 21 tests, 1048 total. Its rule: **"the clocks are the observation and the duration is their summary, so the clocks win — unless the duration is the field the request actually pinned."** Two guard rails it added unprompted: derivation only ever restates a clock that **already exists**, because manufacturing the missing side would silently **stop a running timer** (start set, end null) on an unrelated duration edit; and explicit `null` clears while `undefined` leaves alone. It also restored `startedAt`/`endedAt` in the update **undo** handler — previously absent, so now that an update can move the clocks, an undo would have handed back an entry whose duration and span disagreed. Raised T4.10. |
| 2026-08-14 | **T4.6 done** — the entry dialog, 27 tests, 1027 total. Its 2-of-3 reducer picks **exactly two anchors**, the edited field always one of them and the second by priority `start > end > duration` — which is what makes editing the *end* restate the duration rather than sliding the start. `DurationInput` is remounted via `key={durationEpoch}` **only when the derived duration actually changed**, so typing garbage never remounts the field and never eats the text. `canSave` provably never reads the overlap result, so only a format error can disable Save. Without `rates.view` an explicit line says money is hidden — absent, not silently missing. **It found a silent no-op** (see T4.9). |
| 2026-08-14 | **Phase 4 opened. T4.4 done** — duplicate + copy-day, 34 tests, reusing T4.2's command rather than writing a second. It drew a distinction I had not: a **locked source** is skipped-and-reported, whereas T4.2's bulk grid save fails wholesale — because there the *rows being written* are locked, while here the copy writes nothing to its source and the target day is a fresh draft. Failing a whole day because one entry sits in a closed report would break the button for exactly the people who report most regularly. Idempotency: a non-empty target day is refused (`409 copy_day_target_not_empty`, naming what is already there) with an `allowDuplicates` opt-out — two identical stand-ups are legitimate data, so no natural dedupe key exists. It also declined to wrap the loop in a route-level transaction, because the command bus forks its own EM per command and the outer transaction would enclose nothing: **"the atomicity would be fiction."** |
| 2026-08-14 | **PHASE 3 COMPLETE (static gates).** T3.8 done — board chrome, filters, and the Lista view. Gate: **1056 tests**, core+app typecheck clean, eslint 0 errors, `build:packages` 22/22, `build:app` clean. **It corrected an error in my brief:** I asserted the tasks query already supported `tagIds`; it does not (no filter on the route, POST/DELETE only on `tags/task-assignments`). Rather than silently filtering nothing, it applied the tag filter client-side and made the bound **visible** — the column badge counts matching *loaded* cards while "Show more" keeps counting rows the server still holds. It also found a real bug in the board's inline hours `reduce`: skipping any row with a `parentTaskId` also zeroes a child whose parent is **not** on the board; the shared helper drops only rows whose parent is present in the same set. And it gated the per-column quick-add on `tasks.manage`, which was ungated before. |
| 2026-08-14 | **T3.10 done** — four API gaps closed additively, 32 tests. `doneChildCount` went into the **existing** aggregate via a `LEFT JOIN` on statuses, so no extra round trip and the `id IN (page) OR parent_task_id IN (page)` widening still counts off-page children. Tags land through a second enricher that **chains** with the rollup — two queries per page, never a GET-per-card. **`start-timer` is now atomic**: with `taskId` the entry is created already carrying the task inside the existing transaction, so *no link write exists to fail* — the stranded-timer failure mode is gone rather than handled. `tagIds` needed **no UI change**, because the board and drawer already read it defensively — the seams T3.6/T3.7 deliberately left paid off. |
| 2026-08-14 | **T3.7 done** — the task drawer, 33 tests, **1013 total**. The optimistic-lock token lives in a **ref, not state**, with saves serialized on a chain, so a second blur queued before any re-render still uses the token the first save produced. It proved that with a **mutation test**: swapping the ref for the closure-captured value makes the chaining test fail — i.e. it demonstrated the test would actually catch the bug rather than assuming so. Subtask ticking writes **zero task PUTs** (asserted) — done-ness is a status move, per D-2. The cost badge renders only when an entry actually carried a cost, so an absent cost never becomes a zero. |
| 2026-08-14 | **T4.2 done** (31 tests, 891 total) — the entry lock now sits in the **command layer**, asserted immediately after the row loads and scope is checked, *before* ownership, access, or any request field is read. That binding is what closes R3: a field added to the update command later is frozen without anyone remembering to freeze it. `manage_all` is refused too — an entry frozen in a closed report has already been billed at its `rounded_minutes`. The bulk path checks **two** shapes, because R3's wording is literally that the grid targets `(project, date)` not id: a row whose id resolves to a locked entry, **and an id-less row landing on a cell a locked entry already occupies** — the grid renders one summed value per cell, so a second row there silently restates a billed day. Partial batches fail wholesale, with reasons. Tenant isolation returns **404, not 409**, so a foreign entry never discloses that it exists or which report froze it. Also closed T4.1's rounding gap on `start-timer` / `timer-stop` / `bulk` via a shared `resolveRoundedMinutes`, keeping `roundMinutes` called from exactly one place. |
| 2026-08-14 | **T3.6 done** — the Kanban board. On failure it restores the snapshot **first, unconditionally**, then branches: a 409 raises the conflict bar and offers **no Retry**, because replaying would overwrite the other person's move; every other failure gets one. Echo suppression sweeps entries after 15s so a dropped SSE cannot wedge a later genuinely-remote move. Column hours assert **1:40, not 2:20**, for a parent+child pair — R10 enforced in the UI. Raised the three API gaps now tracked as T3.10. |
| 2026-08-14 | **T3.4b done** (comments, 29 tests) — **890 staff tests, typecheck clean both workspaces, lint at baseline. Phase 3's API layer is complete.** It hand-wrote the route rather than using `makeCrudRoute`, because a CRUD list answers an inaccessible task with an **empty page** — a different response from "no such task", and therefore an existence oracle. It also went stricter than the shared `normalizeAuthorUserId` helper, which lets a non-API-key super-admin post under another user's id: on a client-facing thread that turns the avatar into a claim rather than a fact. Author-only was extended to delete as well as edit — a thread someone else can silently remove from is no more of a record than one they can rewrite. Fixed the two typecheck errors T3.3 flagged (`OpenApiMethodDoc` uses `query`, not `querySchema`; `executedAt` is a required string). |
| 2026-08-14 | **T3.3 done** (drag endpoint, 26 tests). Shares `applyStatusToTask` with `update` rather than duplicating the `closedAt` rule, so a status column stays the single source of done-ness. The optimistic-lock check runs **before any mutation** — a stale header yields 409 with zero writes and **no broadcast**, so the board's rollback is truthful rather than racing a half-applied move. The broadcast itself is fire-and-forget with a logged catch, so a failed SSE emit cannot fail a committed drag. Missing task and inaccessible task return the same body. Scope travels in the event payload because module-level `EmitOptions` accepts only `persistent` — which also resolves the `EmitOptions.tenantId` error T3.5 observed mid-flight. |
| 2026-08-14 | **T3.4a done** (tags, 38 tests) — "not yours" and "does not exist" return **byte-identical 404 bodies**, asserted by a test comparing the two responses; assign retries on a **fresh EM fork** because a rolled-back UoW must not carry persisted rows; refused to tag a locked entry. **T3.5 done** (rollup, 27 tests) — the aggregate widens to `id IN (page) OR parent_task_id IN (page)`, so a board card rolls up children that `topLevelOnly` guarantees are off-page; a same-page-only rollup would make a card's hours depend on the active filter and read as flakiness. Column totals left client-side, with a pure helper that drops rows whose parent is in the same set — R10 made executable. |
| 2026-08-14 | **R3 closed** — list *and* `?id=` detail now narrow through `resolveProjectAccess`; both project pages render `NoProjectAccess`; TC-TT-017 un-quarantined. **T3.1 done** (statuses, W2 registered, template seeded in the project-create transaction). **T4.3 done** (overlaps, 19 tests). **T4.5 done** (`DurationInput`, W3 closed). **T3.2 done** — 60 tests. Spec **D-13** recorded: a Team Member keeps the Projects nav entry and sees only their own projects. |

| When | Event |
|---|---|
| 2026-08-13 | **Phase 3 opened.** T3.1 (task statuses) dispatched as its foundation. T4.3 (overlaps endpoint) and T4.5 (`DurationInput`) pulled forward from Phase 4 — both depend only on finished Phase 1 helpers, so they parallelise against T3.1 on disjoint files rather than idling behind the Kanban chain. |

| When | Event |
|---|---|
| 2026-08-13 | Plan created. Spec corrected: 10 new tables (was miscounted as 7). |
| 2026-08-13 | Phase 1 opened. T1.1–T1.4 dispatched in parallel (disjoint file sets: entities+migration / pure helpers / validators / acl+setup+events). T1.5–T1.6 held until their deps land. |
| 2026-08-13 | T1.4 code complete: 7 ACL ids, 3 employee grants, 14 event ids, `clientBroadcast: true` on `time_task.status_changed` (confirmed a first-class field on the shared `EventDefinition`, declared the same way `customers`/`messages`/`progress` do). `git diff -U0` shows zero deleted lines across the three files — additive against FROZEN/STABLE surfaces as required. `generate` passed; typecheck gate deferred under E1. |
| 2026-08-13 | **E1 raised.** Baseline `node_modules` is 13 days older than `yarn.lock`; repo does not typecheck before our changes. All verification gates deferred until `yarn install`. |
| 2026-08-13 | T1.3 code complete: 18 schemas + 23 types, additive on the three existing entry schemas (`notes` retained beside the new `description`). Isolated `tsc --strict` on the file is clean. Eight judgement calls reported; all accepted, one promoted to watch item W1. |
| 2026-08-13 | T1.2 **done** — 7 helper modules, 104 unit tests green, eslint clean, zero typecheck errors in its own files. Includes the D-7 invariant test: the same six entries grouped by task / person / day all total `841.37`. `round2` shifts through the decimal string rather than `x*100`, so `1.005 → 1.01` instead of the naive `1.00`; `sumAmounts` accumulates in integer cents. |
| 2026-08-13 | **Spec bug found by T1.2 and fixed.** D-10 said "truncate at a word boundary within 20 characters" but gave `NORDVIK-MIGRACJA` for `Nordvik — migracja B2B` — a slug that is *exactly* 20 characters, so the stated rule contradicted its own example. D-10 now specifies a word budget of 19 with 20 as the hard cap. W3 raised on decimal formatting. |
| 2026-08-13 | **E1 resolved and baseline established.** `yarn install --immutable` (exit 0, lockfile untouched) → `build:packages` 22/22 → `generate` → **`yarn test` 29/29**, `yarn lint` 0 errors, `yarn typecheck` 20/22 with the single failure in untracked `agent_orchestrator` WIP. All 594 earlier errors were the stale install. **Phase 1 T1.1–T1.4 all `done`.** |
| 2026-08-13 | **PHASE 2 COMPLETE (with one honest caveat).** 12 tasks + T2.8. Static gates: core+app typecheck clean, eslint 0 errors, `build:packages` 22/22, `build:app` clean, staff **507/507**, repo 27/29 tasks (2 = E4 worker segfault, both 100% green single-process). T2.8 wrote TC-TT-003/004/005/017 — 7 cases, typechecked, linted, all discovered by `playwright --list` — but **there is no environment here to execute them**, so they are recorded as written-not-executed. The second agent stall of the run; work was complete on disk and verified by hand, as with T2.6. |
| 2026-08-13 | T2.12 **done** — `time_project_members.update` command + `PUT`, wired through `makeCrudRoute.actions.update` (the factory command path *is* the guard wiring; `runRouteMutationGuards` would have double-executed guards). URL project id overwrites any body value, so a membership in another project 404s. Audit diff asserted to read `{ assignedEndDate: { from, to } }` — one update, not an add+remove. **Optimistic locking decided deliberately:** the endpoint enforces it (free — `makeCrudRoute` auto-registers the `updated_at` reader, header-driven, so a later single-record editor needs no server change), but the drawer does not send a header, because one Save flushes adds/re-datings/removals as independent writes with no transaction — a mid-batch 409 would leave the team half-applied and the drawer reasoning about a state neither side can describe. Recorded as a comment at the call site. |
| 2026-08-13 | T2.11 **done** — currency is now uniformly read-only on any persisted project, changed only through the explicit action; only the *copy* branches on entry count (no acknowledgement checkbox when there is nothing to relabel). `buildProjectPayload` no longer emits `currencyCode` on update at all. **It found a second-order bug while wiring it:** the change-currency call bumps `updated_at`, but `CrudForm` derives its optimistic-lock header from `initialValues.updatedAt` — so change-currency-then-Save would 409 the user against themselves. Fixed by refreshing initial values after the action, with the conflict bar left as backstop. Regression test pins `Object.keys(payload)` not containing `currencyCode` in update mode for **both** entry states. |
| 2026-08-13 | T2.7 **done** — budget threshold alerts. Subscribes to `staff.timesheets.time_entry.***** (wildcard) so every write path is covered, including future ones, rather than enumerating routes. `persistent: true` because a money alert must survive a worker restart. The marker is claimed by **compare-and-swap** (`WHERE budget_alerted_at_percent IS NOT DISTINCT FROM ?`), so two concurrent entry writes crossing the same threshold raise one alert — and the claim deliberately does **not** bump `updated_at`, which would otherwise 409 an unrelated open project form. Percent comes from the same `computeBudgetBurn` the screen 3/4 burn bar uses, so alert and bar can never disagree. A test pins the D-7 consequence: a 1-minute entry billed as a full hour reaches 83% and alerts, where raw minutes would sit silent at 1%. |
| 2026-08-13 | T2.4 **done** — screen 3 columns (Klient / Stawka / Godziny / Koszt / Budżet burn), bulk actions inline in the toolbar, 26 new pure-function tests. Both mockup notes honoured: **no grand-total row**, and cost via `entryAmount`/`sumAmounts` so rounding stays at the entry. It went further than note 4 and also blocks a cross-**currency** selection, catching R2 at the cheapest point. Money columns gated on `rates.view` on **both** the enricher and the page, so the fields are absent from the payload rather than hidden in the UI. Raised **E3**, which I fixed. |
| 2026-08-13 | T2.2 **done** — screen 4 built end-to-end (customer picker + inline create, D-10 code derivation with live uniqueness, D-3 currency lock with the 409 naming its blocking reports, budget card), 86 i18n keys × 5 locales, staff suite 384, core+app typecheck 0, `build:app` clean. **It found a gap no PLAN row owned:** the project create/update schemas, commands and list projection have zero references to `hourlyRate`, `billableByDefault`, `budget*` or `customerSnapshot`, so zod strips them and every value the form collects is silently dropped on save. Raised as **T2.9** and dispatched immediately; TC-TT-004 cannot pass until it lands. |
| 2026-08-13 | T2.6 agent **stalled** (600s watchdog) with its last words "Now the route test." Rather than restart it, I audited the tree: `NoProjectAccess.tsx`, the route, its test, the subscriber and the `notifications.ts` declaration had all landed. Its own suites run **26/26 green**, its files typecheck clean, and all 15 i18n keys are present across all 5 locales. The only step it never reached was `yarn generate` — run by hand; the subscriber and notification type are now in the generated registries. Marked **done**. A stalled agent is not the same as failed work. |
| 2026-08-13 | T1.8 **done** — D-12 implemented. `graceDays` and `now` are explicit optional context fields rather than resolved inside `resolveProjectAccess`, keeping it testable without a config fake and letting a caller that already read settings skip a second lookup; both default safely so existing callers were untouched. Import direction deliberate: `access.ts` imports the constants **from** `settings.ts`, so `settings.ts` stays free of `data/entities` and remains client-safe. Fails closed on an unparseable date bound or an unusable clock. `canManageAll` returns before memberships are queried, so the window cannot gate a Team Leader. 332 staff tests; core package 9,753. Raised W8. |
| 2026-08-13 | T2.1 **done** — seven routes under a new `Time tracking` group; the projects pages and the editable grid were `git mv`'d rather than stubbed, so nothing regressed between phases. Redirects use per-page `permanentRedirect()` because `backend/middleware.ts` structurally cannot emit 308 (its `PageMiddlewareResult` feeds Next's `redirect()`, a 307); using it would have meant widening a shared type — an "ask first" contract change. Legacy metas deliberately drop `requireFeatures` so a low-privilege user is redirected rather than shown Access Denied. All 5 locales complete. Confirmed at `nav.ts:317-322` that unmet features `continue` past the entry, so TL-only items are **omitted, not disabled**. Raised W7 and R2. |
| 2026-08-13 | **Phase 2 opened.** T2.3 **done** — change-currency command + route, 305 staff tests (291 baseline + 14). Two-phase `withAtomicFlush` with the locked-entry probe in its own phase, sharing the transaction so a report closing concurrently cannot race the check. Undo re-asserts the lock. Read-side `currencyLocked` / `lockedEntryCount` added to the project list via one grouped aggregate. It raised W6, which I fixed immediately. |
| 2026-08-13 | **PHASE 1 COMPLETE.** T1.5 done (13 tests; mutation guard wired on `PUT` via `runRouteMutationGuards`, the current sanctioned wrapper rather than catalog's legacy pair). T1.7 phase gate: `yarn test` **29/29**, `yarn lint` 0 errors, `yarn typecheck` at baseline. 10 tables, 11 columns, 8 pure helper modules, 7 ACL features, 14 events, settings service + API, access resolver + DI + AGENTS.md. No user-visible change, as designed. Raised W5. E2 recorded. |
| 2026-08-13 | T1.6 **done** — `resolveProjectAccess` + `assertProjectAccess`, registered as `timeTrackingAccessResolver` in `di.ts` and documented in the staff `AGENTS.md` DI table (BC surface #9). Reuses `authorizeFeatures` from `@open-mercato/shared/security/featurePolicy` rather than a hand-rolled matcher, so `staff.*` and `staff.timesheets.*` both satisfy the manage feature. Fails closed on missing scope. 119 helper tests / 278 staff tests; core and app both typecheck clean. Raised W4. |
| 2026-08-13 | R1 **closed**. Also recorded three verification lessons, one of them my own diagnostic error — see the R1 entry. |
| 2026-08-13 | T1.1 reported done and was **rejected on review** — its gate (`core build`) does not typecheck. Real typecheck shows six `TS2345` breakages at existing call sites. Raised as R1 and reopened; T1.1's Verify column corrected to a typecheck. Its own follow-up (unregistered optimistic-lock entities) recorded as W2. |
