# Execution plan — port dependabot minor/patch bumps to `develop` (#5812)

## Goal

Land the dependency updates of [PR #5812](https://github.com/open-mercato/open-mercato/pull/5812) (`chore(deps): bump the minor-and-patch group with 47 updates`) on `develop`, the repository's configured base branch, and close the original PR — which dependabot opened against `main`.

## Context

- `main` is fully contained in `develop` (0 commits on `main` that `develop` lacks); `develop` is 160 commits ahead.
- Because dependabot targets `main`, its bumps never reach the branch every feature PR is cut from. Porting is a straight forward-port, not a merge.
- `develop` carries two workspaces that do not exist on `main` — `packages/documents` and `packages/channel-discord` — plus a moved `html-to-text` dependency (`core` → `shared`) and a trimmed root `resolutions` block. The port must therefore be applied by dependency name across the **current** workspace set, not by replaying the diff hunk-for-hunk.
- The root `package.json` `resolutions` block pins several of the bumped packages (`ai`, `esbuild`, `pg`, …). Those pins override every workspace range, so they must move together with the ranges or the bump is inert.
- `.yarnrc.yml` sets `npmMinimalAgeGate: 5d`; any target version published less than five days ago will be rejected at install time and must be left at the highest reachable version, with the gap recorded.

## Scope

- Every workspace `package.json` on `develop` that declares one of the 47 bumped packages.
- The root `resolutions` block.
- `yarn.lock`, regenerated by `yarn install`.
- Closing PR #5812 with a comment pointing at this port.

## Non-goals

- No source changes. If a bumped package needs a code migration, that is a separate PR and this run stops and reports it.
- No major-version bumps — this is dependabot's `minor-and-patch` group only.
- No change to dependabot's own configuration (retargeting it at `develop` is a policy decision for the maintainers; flagged in the PR body, not made here).
- No changes to `external/official-modules`.

## Outcomes worth recording

- **Age gate deviations (2 of 47 updates).** `.yarnrc.yml` sets `npmMinimalAgeGate: 5d`. At run time `jest@30.5.0` and `jest-environment-jsdom@30.5.0` were 4.9 days old and every newer 30.x release was younger still, so the highest installable version is exactly what `develop` already declares — those two ranges are therefore left at `^30.4.2` / `^30.4.1`, unbumped. `lucide-react@1.35.0` was likewise quarantined, and its highest installable version is `1.34.0`, so the range moved `^1.27.0 → ^1.34.0` instead of `^1.35.0`. Dependabot will re-propose all three once they age past the gate; bypassing it with `--no-time-gate` was rejected as a deliberate supply-chain control, not an obstacle.
- **Validation gate result.** `build:packages`, `generate`, `build:packages`, `i18n:check-sync`, `i18n:check-usage`, `typecheck` and `build:app` all pass. `yarn test` reports exactly one failing test out of 11,888 in `@open-mercato/core` — `page.durationEntry.test.tsx › stops counting a cell in the totals once its pending value becomes invalid`. That failure is **pre-existing and unrelated**: it is already tracked as issue #5825 ("timesheet duration-entry test is date-dependent and fails in any week containing a day numbered 2"), today is the 2nd, and reverting this branch's manifests and lockfile to `origin/develop` and reinstalling reproduces the identical failure. Every other workspace suite is green.
- **`lucide-react` moves a committed generated registry.** The bump from `^1.27.0` to `^1.34.0` adds a `Bot` icon that `yarn generate` folds into `packages/ui/src/backend/icons/lucideRegistry.generated.tsx`, a versioned generated registry. It is committed with the bump so `yarn generate` is idempotent on a fresh checkout.
- **The CI `audit` gate was red on a pre-existing advisory.** `node scripts/audit-ci.mjs --severity high` reported two high advisories against `browserslist <=4.28.6` (GHSA-c83g-rgw3-j3cx, GHSA-73wf-gq98-2v4g). `origin/develop`'s lockfile resolves `browserslist` to the same `4.28.2`, and the last scheduled audit on `main` passed on 2026-09-01, so the advisories were disclosed since — this PR is only the first to touch manifests and therefore the first to make `audit-scope` run the job. Fixed by adding a range-less `"browserslist": "4.28.8"` root resolution, matching the repository's existing transitive security-pin pattern.
- **The standalone template had to be re-synced.** `packages/create-app/src/lib/template-dependency-drift.test.ts` fails the moment `packages/create-app/template/package.json.template` and `apps/mercato/package.json` disagree on any shared non-`@open-mercato/*` key — the Template Sync Checklist in `AGENTS.md`. CI caught 23 drifted keys; the local gate did not, because turbo served `create-mercato-app#test` from cache. `yarn template:sync:fix` only knows 5 of those keys, so the remaining 18 were mirrored directly. `template:sync:fix` also wanted to enable `channel_discord` in `template/src/modules.ts`, overwriting a deliberate, documented agent-instruction-budget decision — that edit was reverted as out of scope and pre-existing.
- **Two range bumps are inert behind range-less resolution pins.** The root `resolutions` block pins `mermaid: 11.16.0` and `nodemailer: 9.0.1` without a range, so they override every workspace range. `apps/docs` now asks `mermaid ^11.17.2` and `packages/channel-imap` asks `nodemailer ^9.0.6`, yet the lockfile still resolves `11.16.0` and `9.0.1`. This exactly reproduces the state #5812 produces on `main` — dependabot bumps declared ranges, never pins — so it is carried over unchanged rather than "fixed" inside a port. Whether those two pins are still wanted is a separate maintainer call.

## Risks

- **Resolution drift.** Regenerating `yarn.lock` re-resolves every `^`/`~` range that changed, so the resulting versions may be newer than the ones dependabot pinned against `main`. Acceptable — the ranges are what the repo commits to — but the validation gate is what proves it.
- **Age gate.** A target version younger than 5 days is unreachable; the range is then left at the highest installable version rather than forcing `--no-time-gate`.
- **Runtime breakage from a patch bump.** `next`, `@mikro-orm/*`, `ai`, `pg` and `react` all move. Only the full validation gate (build:packages → generate → i18n checks → typecheck → test → build:app) can catch this, so it runs unabridged.
- **Dependency-consistency guards.** `develop` adds `check:dep-versions`, `check:resolutions` and `packages:check-peer-deps`; a partial port trips them.

## Implementation Plan

### Phase 1: Port the version bumps

1.1 Extract the name → old/new version map from #5812's `package.json` diffs and apply it to every workspace manifest on `develop`, including the `develop`-only `packages/documents` and `packages/channel-discord`, keeping any range already at or above the target.
1.2 Update the root `resolutions` pins for the bumped packages so they do not override the new ranges.
1.3 Regenerate `yarn.lock` with `yarn install`, recording any version blocked by the 5-day age gate.

### Phase 2: Validate

2.1 Run the repository's dependency-consistency guards (`check:dep-versions`, `check:resolutions`, `packages:check-peer-deps`).
2.2 Run the full configured validation gate in order.

### Phase 3: Land and close the original

3.1 Open the port PR against `develop`, then close #5812 with a comment naming this PR.

## Progress

PR: #5835

> Convention: `- [ ]` pending, `- [x]` done. Append ` — <commit sha>` when a step lands. Do not rename step titles.

### Phase 1: Port the version bumps

- [x] 1.1 Apply the bumped ranges to every workspace manifest on develop — 70c37905e0
- [x] 1.2 Update the root resolutions pins — 70c37905e0
- [x] 1.3 Regenerate yarn.lock — 2f8a5b0b38

### Phase 2: Validate

- [x] 2.1 Run the dependency-consistency guards — 2f8a5b0b38
- [x] 2.2 Run the full validation gate — 11b177d14a
- [x] Phase 2 follow-up: regenerate the committed lucide icon registry — 11b177d14a
- [x] Post-review fix: pin browserslist 4.28.8 to clear two high advisories that red the CI `audit` gate — c8b4b8b1f4
- [x] Post-review fix: mirror the ported pins into the create-app standalone template — c95904cd21

### Phase 3: Land and close the original

- [x] 3.1 Close #5812 pointing at the port PR — closed with a comment naming #5835

### Phase 4: Drive CI green (resume)

The port itself is complete; the only red required check left is `test`, and the failure is the
pre-existing time-bomb tracked as #5825 rather than anything this PR changed. It is fixed here
because it reds the required `test` check on every open PR for as long as the rendered week
contains a day numbered 2, so no PR — this one included — can reach green until it is repaired.

- [x] 4.1 Scope the timesheet totals assertion to the totals footer so a day-of-month header can never satisfy it (#5825) — 6a4a357dc9
- [x] 4.2 Merge current develop into the branch — ebb3e29b2a
- [x] 4.3 Re-run the full validation gate — clean, see below
- [x] 4.4 Push and drive every required check to green — `e1e948d186`: 30 pass, 0 fail, 0 pending

### Phase 5: Resolve the develop merge conflict (resume)

The branch fell behind `develop` and went `CONFLICTING`, which blocks the merge queue.

- [x] 5.1 Merge current `develop` and resolve the conflicts — 31aed97afb
- [x] 5.2 Bump the `fast-uri` resolution 3.1.5 -> 3.1.7 to clear four newly-published high advisories that red the `audit` gate
- [x] 5.3 Re-drive the required checks to green on the merge commit — `a83b66606f`: 30 pass, 0 fail, 0 pending

Gate result (local runner — no compose `app` container was up, so `yarn X` rather than
`docker-exec.mjs X`): `build:packages` ✅ · `generate` ✅ (no working-tree churn) · `build:packages`
✅ · `i18n:check-sync` ✅ · `i18n:check-usage` ✅ · `typecheck` ✅ · `build:app` ✅ (real build, cache
miss, 1 m 53 s).

`test`: `@open-mercato/core` is now **1470 suites / 11 920 tests passing, 0 failing** — the #5825
failure is gone. 33 of 34 turbo tasks pass. The one red task is `create-mercato-app#test`, whose
four failures are all the same assertion in `src/lib/template-dev-log-files.test.ts`: `❌ Linux
file-watch limits are too low for Turbopack — fs.inotify.max_user_instances: 128 < 4096`. That is
this machine's sysctl configuration, not the branch: the test refuses to launch the template dev
wrapper before asserting anything about it, and the same task passed on CI in the previous run
(`Failed: @open-mercato/core#test` only). Left to CI rather than mutating the host's system-wide
inotify limits, which is outside this PR's scope.

The mutation check on 4.1: deleting the `dropDirtyCell(projectId, dateKey)` guard from
`handleCellBlur` (`page.tsx:422`) reddens the rewritten assertion again (it finds `2` in both the
day-total and grand-total cells), so scoping the query narrowed it to the calendar noise without
weakening what it pins.
