# Execution plan — make the invitation email path work under the tenant-scoped credential resolution this PR introduces (adopted from PR #4471)

**Origin:** adopted — reconstructed by `om-auto-continue-pr` on 2026-08-21 because PR #4471 carried no execution plan.
**PR:** #4471 · **Branch:** `carry/pr-2448-ready` · **Base:** `develop`
**Author:** @pmadajthey (carried forward) — this plan interprets their intent; correct it by editing this file or commenting on the PR.

## 🎯 Goal

Leave PR #4471 with its outbound-email work intact and its `ephemeral-integration` shard green, by fixing the one defect that makes every user-invitation request fail under the tenant-scoped credential resolution the PR introduces.

## Scope

`packages/core/src/modules/communication_channels/` — specifically the test-channel seeding in `setup.ts` `seedDefaults()`, plus unit coverage for the seeded state it produces.

## Non-goals

- Re-QAing the rest of the 720-file diff. Manual QA of the system-email path is already done and reported (#issuecomment-5367408281); this run does not extend it.
- Weakening the fail-closed branch in `resolveTenantCredentials`. It was verified as correct during manual QA and protects tenant isolation.
- The stored `notifications` / `delivery_strategies` config pinning `ops@your-domain.com`, which makes notification email 403 on dev instances. Pre-existing on `develop`, reproducible there — belongs in its own issue.
- Playwright hanging at 100% CPU during collection when `--filter` selects a subset. Unrelated to this PR — belongs in its own issue.
- The design question of whether a failed invitation email should roll the invitation back and return 502 at all. Raised for a reviewer on the PR; changing it is not this run's call.
- Setting `qa-approved`. Declined by the maintainer; the `needs-qa` gate stays.

## Evidence

| Conclusion | Drawn from | Confidence |
|---|---|---|
| Three named tests fail on head `daa9ea287` | `gh pr checks 4471`, run 32456198405 job 96694565189, whose `headSha` equals the PR head | high |
| The failures are not stale expectations | the diff adds none of the three spec files but does change `api/admin/users-invite.ts`, `api/portal/users-invite.ts`, `lib/invitationEmail.ts`, `api/signup.ts` | high |
| Adding `tenantId` moves invite email onto the tenant-scoped branch | `sendSystemEmail`: `payload.tenantId ? resolveTenantCredentials(...) : resolveEnvCredentials(...)`, and the one-line diff in `users-invite.ts` | high |
| The harness seeds an email channel with no credentials | `setup.ts` `seedDefaults()` creates the `__test_seed__` `CommunicationChannel` and never stores credentials; ephemeral DB shows `__test_seed__` and `resend` rows both with `credentials_ref` NULL | high |
| That combination throws and 502s the invite | `resolveTenantCredentials` throws `SYSTEM_EMAIL_CREDENTIALS_NOT_CONFIGURED` when `hasTenantChannel` and no credentials; `users-invite.ts` catches, rolls back, returns 502 | high |
| It reproduces outside Playwright | live POST to `/api/customer_accounts/admin/users-invite` on an ephemeral env returned HTTP 502 and left `customer_user_invitations` at 0 rows | high |

## Assumptions

- Seeding credentials for the test channel is the intended fix, rather than exempting `__test_seed__` from credential resolution. Chosen because it leaves production logic untouched and makes the seeded tenant represent a *properly configured* tenant, which is also the only one of the three credential cases the integration suite currently never exercises. The alternative is a one-line guard in `system-email.ts` and stays available if a reviewer prefers it.
- The credential shape `{ testSeed: true, fromAddress }` is correct, because it is exactly what the test-seed provider's own `resolveCredentials` returns in `ensureTestSeedAdapterRegistered()`.
- The `resend` channel row seen in the ephemeral database is inert for this path, because `findTenantSystemEmailChannel` filters by `providerKey` and `SYSTEM_EMAIL_PROVIDER` is `__test_seed__` there.

## Risks

- The same shape — a channel row without credentials — can plausibly occur on a real instance that upgraded onto the Communications Hub. If so, this fix makes CI green without addressing the production consequence, and the design question in Non-goals becomes the more important one. Flagged for the reviewer rather than silently resolved here.
- Only the three named tests are verified against; other suites touching invitation email may reveal further fallout under the full gate.

## Progress

> Convention: `- [ ]` pending, `- [x]` done. Append ` — <commit sha>` when a step lands. Do not rename step titles.

### Phase 1: Already landed on this PR (reconstructed)

- [x] 1.1 Pluggable outbound email providers, Communications Hub routing, Resend and SES packages, #5010 credential-resolution remediation, base sync onto current `develop` — daa9ea287

### Phase 2: Restore invitation email under tenant-scoped credentials

- [x] 2.1 Seed credentials for the `__test_seed__` channel in `communication_channels` `seedDefaults()`, idempotently and inside the existing test-only guard — cf7b6d2
- [x] 2.2 Add unit coverage asserting the seeded channel resolves credentials, so the harness contract cannot regress silently — cf7b6d2
- [x] 2.3 Verify a live invitation request returns 201 instead of 502 against an ephemeral environment — cf7b6d2 (live: HTTP 201, invitation persisted; before the fix the same call returned 502 and left 0 rows)

### Phase 3: Review pass

- [x] 3.1 Code review of this resume's delta; both findings addressed — 7ffc010 (unguarded credential storage could abort tenant init; guard second conjunct and best-effort path were untested)

### Phase 4: Re-sync onto `develop` (2026-08-25 resume)

> Added by `om-auto-continue-pr` on 2026-08-25. Phases 1–3 were complete and the PR body read
> `Status: complete`, but the PR had drifted out of a mergeable state: GitHub reported
> `mergeable: CONFLICTING` / `mergeStateStatus: DIRTY` against `develop`, which had advanced past
> `fc5e3c744` (most visibly with the `channel-discord` provider in #4391, the `AlertDescription`
> element-type change in #5487 and the devices camelCase response change in #5513). A PR that
> cannot merge is not finished, so this phase re-syncs the branch and re-establishes the gate on
> the merged result. It reopens none of the email-provider work of Phases 1–3.

- [x] 4.1 Merge current `origin/develop` into the PR branch and resolve the conflicts faithfully — 69466b38c (single conflict, `UPGRADE_NOTES.md`: both sides added sections under `0.7.0 → 0.7.1 (unreleased)`, so both are kept)
- [x] 4.2 Re-run the full `validation.commands` gate on the merged tree and fix any integration fallout — 2c456ab27 (all eight commands exit 0 locally on the merged tree; `yarn test` ran 36/36 turbo tasks green, `@open-mercato/core` alone at 1,434 suites / 11,566 tests; no fallout to fix, so no code commit was needed)
- [x] 4.3 Authoritative review pass (`om-auto-review-pr 4471 --autofix`) on the re-synced head — no blockers, majors, minors or nits; submitted as a comment review rather than a formal approval because this run pushed commits to the branch and @pkarw asked that approval come from @Kapsik89 or @alinadivante
