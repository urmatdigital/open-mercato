{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://open-mercato.dev/schemas/skills/tiers.schema.json",
  "title": "Open Mercato skills tier manifest",
  "description": "Manifest mapping skill folders under .ai/skills/ to install tiers consumed by scripts/install-skills.mjs.",
  "type": "object",
  "additionalProperties": false,
  "required": ["default", "tiers"],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "default": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Tier names installed when install-skills.mjs is invoked with no flags."
    },
    "agents": {
      "type": "object",
      "additionalProperties": false,
      "description": "Agent harness settings for install-skills.mjs. Skills always install into the canonical .agents/skills/ directory; per-agent links are created only for agents that cannot read it.",
      "properties": {
        "ignore": {
          "type": "array",
          "description": "Agents whose directories install-skills.mjs must never write. Overridden by --ignore-agents.",
          "items": {
            "type": "string",
            "enum": ["claude-code", "codex", "cursor"]
          }
        }
      }
    },
    "external": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "skills"],
      "description": "Skills owned by an external collection and installed via `npx skills add` instead of local symlinks. Folders under .ai/skills/ matching these names are repo-local overrides, not installable skills.",
      "properties": {
        "source": {
          "type": "string",
          "minLength": 1,
          "description": "The skills package source, e.g. a GitHub owner/repo."
        },
        "description": {
          "type": "string"
        },
        "skills": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9._-]*$"
          }
        }
      }
    },
    "tiers": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*$"
      },
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["description", "skills"],
        "properties": {
          "description": {
            "type": "string",
            "minLength": 1
          },
          "skills": {
            "type": "array",
            "minItems": 1,
            "items": {
              "type": "string",
              "pattern": "^[a-z0-9][a-z0-9._-]*$"
            }
          }
        }
      }
    }
  }
}
