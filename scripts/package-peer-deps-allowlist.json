{
  "version": 1,
  "note": "Entries here are unmet peer dependencies that predate the gate. New violations must be fixed, not appended. Regenerate with `yarn packages:check-peer-deps --update-allowlist` only after confirming each new reason.",
  "followUp": "react-is: `@open-mercato/ui` peer-requires react-is without shipping it, so every package depending on @open-mercato/ui inherits an undeclared peer. Scaffolded apps satisfy it transitively today (recharts, react-transition-group), so it is not a live break — but it is the same shape of risk as the happy-dom regression this gate exists to catch, and it should be closed by declaring react-is in @open-mercato/ui's dependencies. Doing that removes the eleven react-is entries below in one step.",
  "accepted": {
    "@open-mercato/ai-assistant -> react-markdown -> @types/react": "Type-only peer; the scaffolded app declares @types/react itself and a runtime build never resolves it",
    "@open-mercato/channel-apns -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/channel-apns -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above",
    "@open-mercato/channel-discord -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/channel-discord -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above",
    "@open-mercato/channel-expo -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/channel-expo -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above",
    "@open-mercato/channel-fcm -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/channel-fcm -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above",
    "@open-mercato/channel-gmail -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/channel-gmail -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above",
    "@open-mercato/channel-imap -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/channel-imap -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above",
    "@open-mercato/checkout -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/checkout -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above",
    "@open-mercato/documents -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above",
    "@open-mercato/documents -> @tiptap/react -> @types/react": "Type-only peer; the scaffolded app declares @types/react itself and a runtime build never resolves it",
    "@open-mercato/documents -> @tiptap/react -> @types/react-dom": "Type-only peer; the scaffolded app declares @types/react-dom itself and a runtime build never resolves it",
    "@open-mercato/enterprise -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/enterprise -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above",
    "@open-mercato/gateway-stripe -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/gateway-stripe -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above",
    "@open-mercato/storage-s3 -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/storage-s3 -> @open-mercato/core -> @open-mercato/ui": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/storage-s3 -> @open-mercato/core -> react": "React runtime is owned by the host application, which declares react directly",
    "@open-mercato/storage-s3 -> @open-mercato/core -> react-dom": "React runtime is owned by the host application, which declares react-dom directly",
    "@open-mercato/sync-akeneo -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/sync-akeneo -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above",
    "@open-mercato/ui -> react-markdown -> @types/react": "Type-only peer; the scaffolded app declares @types/react itself and a runtime build never resolves it",
    "@open-mercato/webhooks -> @open-mercato/core -> @open-mercato/ai-assistant": "Workspace peer the scaffolded app installs directly from its own package.json",
    "@open-mercato/webhooks -> @open-mercato/ui -> react-is": "Pre-existing: satisfied transitively in the scaffolded app today. Not a fresh break, but it is undeclared — see the `followUp` note above"
  }
}
