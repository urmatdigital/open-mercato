#!/usr/bin/env node

console.log(`Agent skills are not set up for this app yet.

\`yarn install-skills\` needs the agentic setup files. Run:

    yarn mercato agentic:init

then retry. Scaffolding with --skip-agentic-setup or --agents none intentionally
leaves this app without agent tooling.`)
