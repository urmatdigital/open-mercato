/**
 * Side-effect entry point that populates the Activity Registry with the
 * built-in activity types. Import this module wherever registry lookups must
 * be guaranteed (executor, worker, validators).
 */

import './activity-types'
