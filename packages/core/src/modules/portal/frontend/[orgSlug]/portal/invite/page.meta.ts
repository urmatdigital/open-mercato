import type { PageMetadata } from '@open-mercato/shared/modules/registry'

export const metadata: PageMetadata = {
  titleKey: 'portal.nav.invite',
  title: 'Accept Invitation',
  navHidden: true,
}

export default metadata
