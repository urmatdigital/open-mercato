{
  "name": "@open-mercato/eslint-plugin-ds",
  "version": "0.7.0",
  "private": true,
  "description": "Structural ESLint rules enforcing the Open Mercato design system (empty states, page wrappers, raw tables, loading states, status badges, hardcoded status colors, legacy Alert variant)",
  "type": "module",
  "main": "index.js",
  "exports": {
    ".": "./index.js"
  },
  "scripts": {
    "test": "node --require ../../scripts/typescript-js-require-hook.cjs --test ./tests/rules.test.js"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "^8.65.0",
    "eslint": "^9.39.4"
  },
  "peerDependencies": {
    "eslint": ">=9"
  }
}
